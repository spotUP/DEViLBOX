const __vite__mapDeps=(i,m=__vite__mapDeps,d=(m.f||(m.f=["assets/main-BbV5VyEH.js","assets/client-DHYdgbIN.js","assets/vendor-ui-AJ7AT9BN.js","assets/vendor-react-Dgd_wxYf.js","assets/vendor-utils-a-Usm5Xm.js","assets/vendor-tone-48TQc1H3.js","assets/main-c6CPs1E0.css","assets/VGMParser-ecNUSeGy.js","assets/YMParser-BM7sqE7t.js","assets/NSFParser-B061ToHI.js","assets/Cpu6502-BtrQdhpg.js","assets/SAPParser-BGWRZqIN.js","assets/AYParser-OiqP0bGC.js","assets/KSSParser-BOCmcQSR.js","assets/HESParser-DoV_VCFe.js","assets/GBSParser-CIBIThAN.js","assets/SPCParser-CuBGzKRA.js","assets/MDXParser-BUCoXj-m.js","assets/PMDParser-BpqUPIWJ.js","assets/S98Parser-CJ4PGAbw.js","assets/SNDHParser-DCHxPthl.js","assets/OpenMPTConverter--7ALLBUZ.js","assets/OpenMPTSoundlib-RubRPKN7.js","assets/S3MParser-DkDPrStE.js","assets/ITParser-z_xBMeYv.js","assets/KlysParser-D34hihxt.js","assets/XTrackerParser-BkoYrZTM.js","assets/DefleMaskToSong-Y7bxtjqc.js","assets/FurnaceToSong-MoKRY9wG.js","assets/OktalyzerParser-CvKeZnZ9.js","assets/MEDParser-DKJWsJvQ.js","assets/DigiBoosterParser-D-3oOuS8.js","assets/DeltaMusic2Parser-BNBAj9qa.js","assets/FCParser-DLdFz_2C.js","assets/SoundMonParser-uW5D_Bww.js","assets/SidMon1Parser-C0NDTL2s.js","assets/SidMon2Parser-C0Bch-oo.js","assets/FredEditorParser-CSrVkxu_.js","assets/FredEditorEncoder-rSEnxCqL.js","assets/SoundFXParser-Dq4k3u4G.js","assets/SoundFXEncoder-BhznWvHj.js","assets/JamCrackerParser-BM0lewvd.js","assets/QuadraComposerParser-Dz3Cm10H.js","assets/QuadraComposerEncoder-JE3-GGZi.js","assets/AMOSMusicBankParser-BeX5Hrg-.js","assets/SonicArrangerParser-BntgX101.js","assets/InStereo2Parser-DMckRwPw.js","assets/InStereo1Parser-Db-e2aBz.js","assets/HippelCoSoParser-DydFFz-Y.js","assets/RobHubbardParser-DczqYvyl.js","assets/TFMXParser-DNc9g4Yy.js","assets/TFMXEncoder-CCEY1ckI.js","assets/DigitalMugicianParser-DlVBZ5dm.js","assets/SIDParser-7jTsMEg5.js","assets/DavidWhittakerParser-BhHjuDNq.js","assets/ArtOfNoiseParser-4YM8QbJl.js","assets/GraoumfTracker2Parser-kkNVCBx4.js","assets/PumaTrackerParser-Bhgp5dgm.js","assets/SynthesisParser-K-SNBo6N.js","assets/DigitalSoundStudioParser-CT3T15d5.js","assets/MusicAssemblerParser-COvMS3Ra.js","assets/Composer667Parser-D4s4kZ0b.js","assets/Composer667Encoder-o8O1EiUD.js","assets/BenDaglishParser-B8qfgNJC.js","assets/SawteethParser-Bf_OwRXQ.js","assets/SoundControlParser-GBUd89o-.js","assets/SoundFactoryParser-CbCkFfJ2.js","assets/ActionamicsParser-ClbLJkoh.js","assets/ActionamicsEncoder-CxUzspTM.js","assets/ActivisionProParser-Dzt6J4vS.js","assets/RonKlarenParser-Dv0g6N1b.js","assets/UNICParser-BSHcmGDb.js","assets/MTMParser-B0eVSB7X.js","assets/Format669Parser-D4FObzyF.js","assets/FARParser-CcW-dv38.js","assets/PLMParser-B18M4plG.js","assets/ULTParser-QuM6OqAJ.js","assets/RTMParser-CNEnSXvi.js","assets/DSMParser-WhYn6SNV.js","assets/DTMParser-CuQm3u8Z.js","assets/STMParser-7BTVgRh_.js","assets/STXParser-DT6Ybgnl.js","assets/NRUParser-BWRqipWt.js","assets/PTMParser-gKUwOnXN.js","assets/GDMParser-BUfX8k8-.js","assets/STKParser-BGUKbLEP.js","assets/STPParser-BC6euN58.js","assets/MDLParser-Di9ZWSE7.js","assets/AMFParser-C-PGXJmB.js","assets/ImagoOrpheusParser-DMCtLcLW.js","assets/CDFM67Parser-DguyM7Ic.js","assets/CDFM67Encoder-Dw1zKW5X.js","assets/KarlMortonParser-NBEvWyW9.js","assets/XMFParser-B98Hvq95.js","assets/XMFEncoder-q5F4y1aF.js","assets/MadTracker2Parser-pgsRrRhj.js","assets/PSMParser-CoFrjjEl.js","assets/IffSmusParser-CFXS4lKv.js","assets/SonixMusicDriverParser-CmlnKfh2.js","assets/MFPParser-DrcqVC3G.js","assets/DeltaMusic1Parser-DoSsyp-W.js","assets/RichardJosephParser-BsgWQiRW.js","assets/PT36Parser-DTMy0fTA.js","assets/DaveLoweParser-kVqiosPY.js","assets/JeroenTelParser-BxTylhed.js","assets/ZoundMonitorParser-DsMlDay5.js","assets/FuturePlayerParser-C5CitIHh.js","assets/TCBTrackerParser-CgEzAn7h.js","assets/JasonPageParser-iY2Ju95V.js","assets/SteveTurnerParser-DtqwpMhs.js","assets/KrisHatlelidParser-CktN4N79.js","assets/NovoTradePackerParser-BcwFXFfo.js","assets/FashionTrackerParser-D2Pq3KR7.js","assets/ThomasHermannParser-cNwmnaZM.js","assets/CoreDesignParser-O97ycm3Z.js","assets/SoundPlayerParser-Cxb-R9Ob.js","assets/NickPellingPackerParser-BFm9SVMQ.js","assets/WallyBebenParser-DxIKdBW6.js","assets/DesireParser-D0wOvieS.js","assets/MartinWalkerParser-ZR0uJogW.js","assets/PaulShieldsParser-Ca-bhh1B.js","assets/PaulRobothamParser-BKP5I462.js","assets/AndrewPartonParser-Cr3Dooet.js","assets/DigitalSonixChromeParser-D5cpGDHk.js","assets/KimChristensenParser-BX2Yl8Rp.js","assets/AshleyHoggParser-yHRHZP8w.js","assets/JanneSalmijarviParser-DbO0Ktwz.js","assets/StartrekkerAMParser-By5VEfa4.js","assets/MaximumEffectParser-DiJbXk--.js","assets/PaulTongeParser-B7DYXn39.js","assets/WantedTeamDaveLoweParser-CzkVhSGq.js","assets/SeanConnollyParser-FMmgxygZ.js","assets/SimpleAmigaStubParser-CkgiCTxp.js","assets/SCUMMParser-NzJ1aYWl.js","assets/SCUMMEncoder-DscPMg-Y.js","assets/SunTronicParser-BCdHs8Se.js","assets/EarAcheParser-DNo7UusZ.js","assets/EarAcheEncoder-DTpkdNs_.js","assets/MusicMakerParser-Bj1gvTss.js"])))=>i.map(i=>d[i]);
import { am as __vitePreload } from "./main-BbV5VyEH.js";
import "./client-DHYdgbIN.js";
import "./vendor-ui-AJ7AT9BN.js";
import "./vendor-react-Dgd_wxYf.js";
import "./vendor-utils-a-Usm5Xm.js";
import "./vendor-tone-48TQc1H3.js";
async function callUADE(ctx) {
  const { parseUADEFile } = await __vitePreload(async () => {
    const { parseUADEFile: parseUADEFile2 } = await import("./main-BbV5VyEH.js").then((n) => n.ji);
    return { parseUADEFile: parseUADEFile2 };
  }, true ? __vite__mapDeps([0,1,2,3,4,5,6]) : void 0);
  return parseUADEFile(
    ctx.buffer,
    ctx.originalFileName,
    ctx.prefs.uade ?? "enhanced",
    ctx.subsong,
    ctx.preScannedMeta,
    ctx.companionFiles
  );
}
function injectUADEPlayback(result, ctx) {
  if (result.uadePatternLayout && !result.uadeEditableFileData) {
    result.uadeEditableFileData = ctx.buffer.slice(0);
    result.uadeEditableFileName = ctx.originalFileName;
  }
  if (ctx.companionFiles && ctx.companionFiles.size > 0) {
    result.uadeCompanionFiles = ctx.companionFiles;
  }
  return result;
}
async function withNativeDefault(prefKey, ctx, nativeParse) {
  if (ctx.prefs[prefKey] === "uade") return callUADE(ctx);
  return injectUADEPlayback(await nativeParse(ctx.buffer, ctx.originalFileName), ctx);
}
async function withNativeThenUADE(prefKey, ctx, nativeParse, parserName, opts) {
  if (opts == null ? void 0 : opts.injectUADE) {
    try {
      const bytes = (opts == null ? void 0 : opts.usesBytes) || (opts == null ? void 0 : opts.isFormat) ? new Uint8Array(ctx.buffer) : void 0;
      if (!(opts == null ? void 0 : opts.isFormat) || !bytes || opts.isFormat(bytes)) {
        const input = (opts == null ? void 0 : opts.usesBytes) || (opts == null ? void 0 : opts.isFormat) ? bytes : ctx.buffer;
        const result = await nativeParse(input, ctx.originalFileName);
        if (result) {
          if (!result.uadeEditableFileData) {
            result.uadeEditableFileData = ctx.buffer.slice(0);
            result.uadeEditableFileName = ctx.originalFileName;
          }
          if (result.instruments.length === 0) {
            result.instruments = [{
              id: 0,
              name: "UADE Audio",
              type: "synth",
              synthType: "UADEEditableSynth",
              effects: [],
              volume: -6,
              pan: 0,
              uade: {
                type: "uade",
                fileData: ctx.buffer.slice(0),
                filename: ctx.originalFileName,
                subsongCount: 1,
                currentSubsong: 0,
                metadata: { player: "Unknown", formatName: "Unknown", minSubsong: 0, maxSubsong: 0 }
              }
            }];
          } else {
            for (const inst of result.instruments) {
              inst.synthType = "UADEEditableSynth";
            }
          }
          return injectUADEPlayback(result, ctx);
        }
      }
    } catch (err) {
      console.warn(`[${parserName}] Native parse failed for ${ctx.originalFileName}:`, err);
    }
    return callUADE(ctx);
  }
  if (ctx.prefs[prefKey] === "native") {
    try {
      const bytes = (opts == null ? void 0 : opts.usesBytes) || (opts == null ? void 0 : opts.isFormat) ? new Uint8Array(ctx.buffer) : void 0;
      if ((opts == null ? void 0 : opts.isFormat) && bytes && !opts.isFormat(bytes)) {
        return callUADE(ctx);
      }
      const input = (opts == null ? void 0 : opts.usesBytes) || (opts == null ? void 0 : opts.isFormat) ? bytes : ctx.buffer;
      const result = await nativeParse(input, ctx.originalFileName);
      if (result) {
        const totalNotes = result.patterns.reduce((sum, p) => sum + p.channels.reduce((cSum, ch) => cSum + ch.rows.filter((r) => r.note > 0).length, 0), 0);
        if (totalNotes === 0) {
          console.warn(`[${parserName}] Native parser returned 0 notes for ${ctx.originalFileName}, falling back to UADE`);
          return callUADE(ctx);
        }
        return injectUADEPlayback(result, ctx);
      }
    } catch (err) {
      console.warn(`[${parserName}] Native parse failed for ${ctx.originalFileName}, falling back to UADE:`, err);
    }
  }
  return callUADE(ctx);
}
function getBasename(filename) {
  return (filename.split("/").pop() ?? filename).split("\\").pop().toLowerCase();
}
async function tryChipDumpParse(buffer, filename, originalFileName) {
  if (/\.(vgm|vgz)$/.test(filename)) {
    const { parseVGMFile } = await __vitePreload(async () => {
      const { parseVGMFile: parseVGMFile2 } = await import("./VGMParser-ecNUSeGy.js");
      return { parseVGMFile: parseVGMFile2 };
    }, true ? __vite__mapDeps([7,0,1,2,3,4,5,6]) : void 0);
    return parseVGMFile(buffer, originalFileName);
  }
  if (/\.ym$/.test(filename)) {
    const { parseYMFile } = await __vitePreload(async () => {
      const { parseYMFile: parseYMFile2 } = await import("./YMParser-BM7sqE7t.js");
      return { parseYMFile: parseYMFile2 };
    }, true ? __vite__mapDeps([8,0,1,2,3,4,5,6]) : void 0);
    return parseYMFile(buffer, originalFileName);
  }
  if (/\.nsfe?$/.test(filename)) {
    const { parseNSFFile } = await __vitePreload(async () => {
      const { parseNSFFile: parseNSFFile2 } = await import("./NSFParser-B061ToHI.js");
      return { parseNSFFile: parseNSFFile2 };
    }, true ? __vite__mapDeps([9,0,1,2,3,4,5,6,10]) : void 0);
    return parseNSFFile(buffer, originalFileName);
  }
  if (/\.sap$/.test(filename)) {
    const { parseSAPFile } = await __vitePreload(async () => {
      const { parseSAPFile: parseSAPFile2 } = await import("./SAPParser-BGWRZqIN.js");
      return { parseSAPFile: parseSAPFile2 };
    }, true ? __vite__mapDeps([11,0,1,2,3,4,5,6,10]) : void 0);
    return parseSAPFile(buffer, originalFileName);
  }
  if (/\.ay$/.test(filename)) {
    const { parseAYFile } = await __vitePreload(async () => {
      const { parseAYFile: parseAYFile2 } = await import("./AYParser-OiqP0bGC.js");
      return { parseAYFile: parseAYFile2 };
    }, true ? __vite__mapDeps([12,0,1,2,3,4,5,6]) : void 0);
    return parseAYFile(buffer, originalFileName);
  }
  if (/\.kss$/.test(filename)) {
    const { parseKSSFile } = await __vitePreload(async () => {
      const { parseKSSFile: parseKSSFile2 } = await import("./KSSParser-BOCmcQSR.js");
      return { parseKSSFile: parseKSSFile2 };
    }, true ? __vite__mapDeps([13,0,1,2,3,4,5,6]) : void 0);
    return parseKSSFile(buffer, originalFileName);
  }
  if (/\.hes$/.test(filename)) {
    const { parseHESFile } = await __vitePreload(async () => {
      const { parseHESFile: parseHESFile2 } = await import("./HESParser-DoV_VCFe.js");
      return { parseHESFile: parseHESFile2 };
    }, true ? __vite__mapDeps([14,0,1,2,3,4,5,6]) : void 0);
    return parseHESFile(buffer, originalFileName);
  }
  if (/\.gbs$/.test(filename)) {
    const { parseGBSFile } = await __vitePreload(async () => {
      const { parseGBSFile: parseGBSFile2 } = await import("./GBSParser-CIBIThAN.js");
      return { parseGBSFile: parseGBSFile2 };
    }, true ? __vite__mapDeps([15,0,1,2,3,4,5,6]) : void 0);
    return parseGBSFile(buffer);
  }
  if (/\.spc$/.test(filename)) {
    const { parseSPCFile } = await __vitePreload(async () => {
      const { parseSPCFile: parseSPCFile2 } = await import("./SPCParser-CuBGzKRA.js");
      return { parseSPCFile: parseSPCFile2 };
    }, true ? __vite__mapDeps([16,0,1,2,3,4,5,6]) : void 0);
    return parseSPCFile(buffer);
  }
  if (/\.mdx$/.test(filename)) {
    const { parseMDXFile } = await __vitePreload(async () => {
      const { parseMDXFile: parseMDXFile2 } = await import("./MDXParser-BUCoXj-m.js");
      return { parseMDXFile: parseMDXFile2 };
    }, true ? __vite__mapDeps([17,0,1,2,3,4,5,6]) : void 0);
    return parseMDXFile(buffer);
  }
  if (/\.(m|m2|mz|pmd)$/.test(filename)) {
    const { parsePMDFile } = await __vitePreload(async () => {
      const { parsePMDFile: parsePMDFile2 } = await import("./PMDParser-BpqUPIWJ.js");
      return { parsePMDFile: parsePMDFile2 };
    }, true ? __vite__mapDeps([18,0,1,2,3,4,5,6]) : void 0);
    return parsePMDFile(buffer);
  }
  if (/\.s98$/.test(filename)) {
    const { parseS98File } = await __vitePreload(async () => {
      const { parseS98File: parseS98File2 } = await import("./S98Parser-CJ4PGAbw.js");
      return { parseS98File: parseS98File2 };
    }, true ? __vite__mapDeps([19,0,1,2,3,4,5,6]) : void 0);
    return parseS98File(buffer);
  }
  if (/\.sndh$/.test(filename)) {
    const { parseSNDHFile } = await __vitePreload(async () => {
      const { parseSNDHFile: parseSNDHFile2 } = await import("./SNDHParser-DCHxPthl.js");
      return { parseSNDHFile: parseSNDHFile2 };
    }, true ? __vite__mapDeps([20,0,1,2,3,4,5,6]) : void 0);
    return parseSNDHFile(buffer);
  }
  return null;
}
const UADE_ONLY_PREFIXES = [
  // ArtAndMagic, AMOS, Sierra-AGI
  "aam.",
  "abk.",
  "agi.",
  // AshleyHogg
  "ah.",
  // ActionAmics (prefix form), BeathovenSynthesizer
  "ast.",
  "bss.",
  "bvs.",
  // ChipTracker (Chuck Biscuits Archive)
  "chip.",
  "cba.",
  // CoreDesign
  "cd.",
  // FutureComposer-BSI, custom
  "bfc.",
  "bsi.",
  "fc-bsi.",
  "cus.",
  "cust.",
  "custom.",
  // DaveLowe WTD variant
  "dlw.",
  // DynamicSynthesizer, DariusZendeh (DavidHanney handled by DavidHanneyParser)
  "dns.",
  "dz.",
  "mkiio.",
  // EarAche, EMS (mg.* was GlueMon alias — handled by GlueMonParser; ea.* = EarAche still UADE)
  "ea.",
  "ems.",
  "emsv6.",
  // ForgottenWorlds (GlueMon handled by GlueMonParser)
  "fw.",
  // FredEditor/Monitor (fredmon prefix)
  "fredmon.",
  // FuturePlayer2 (fp2 prefix, also pat. follin)
  "fp2.",
  // HowieDavies, MajorTom variants
  "hd.",
  "hn.",
  "thn.",
  "mtp2.",
  "arp.",
  // JochenHippel base (hip.*, mcmd.*, sog.*) — different from CoSo and 7V
  "hip.",
  "mcmd.",
  "sog.",
  // MarkII, MusiclineEditor (MikeDavies handled by AmigaFormatParsers with classic mode)
  "mk2.",
  "mkii.",
  "ml.",
  // MaxTrax
  "mxt.",
  // Silmarils
  "mok.",
  "sil.",
  // NoiseTracker/ProTracker variant
  "nt.",
  // NTSP
  "ntsp.",
  // PaulSummers
  "psum.",
  // Pokeynoise
  "pn.",
  // RiffRaff, SeanConnolly (sc2 = SeanConnolly2)
  "riff.",
  "s-c.",
  "scn.",
  "sc2.",
  // RobHubbardST
  "rhst.",
  "rho.",
  // RonKlaren
  "rkl.",
  // SonicArranger variants, SpeedyA1System
  "sa-p.",
  "lion.",
  "sa_old.",
  "sas.",
  // SCUMM, SynthDream
  "scumm.",
  "sdr.",
  // SoundProgrammingLanguage, SoundImages
  "spl.",
  "tw.",
  // SUN-Tronic, SynTracker, StoneTracker
  "sun.",
  "synmod.",
  "st.",
  // TimFollin
  "tf.",
  // AHX thx prefix (AbyssHighestExperience — thx.* prefix form of AHX)
  "thx.",
  // TFMX variant prefixes (all handled by UADE)
  "tfhd1.5.",
  "tfhd7v.",
  "tfhdpro.",
  "tfmx1.5.",
  "tfmx7v.",
  "tfmxpro.",
  // TomyTracker
  "tomy.",
  // VoodooSupremeSynthesizer
  "vss.",
  // ── PTK-Prowiz packed MOD formats ──────────────────────────────────────
  // All these go through UADE's PTK-Prowiz replayer (ProWizard unpacker).
  // Noisepacker
  "np.",
  "np1.",
  "np2.",
  "noisepacker2.",
  "np3.",
  "noisepacker3.",
  // ProPacker / PowerPacker
  "pp10.",
  "pp20.",
  "pp21.",
  "pp30.",
  "ppk.",
  // The Player
  "p10.",
  "p21.",
  "p30.",
  "p40a.",
  "p40b.",
  "p41a.",
  "p4x.",
  "p50a.",
  "p5a.",
  "p5x.",
  "p60.",
  "p60a.",
  "p61.",
  "p61a.",
  "p6x.",
  // ProRunner
  "pr1.",
  "pr2.",
  "pru.",
  "pru1.",
  "pru2.",
  "prun.",
  "prun1.",
  "prun2.",
  // Promizer
  "pm.",
  "pm0.",
  "pm01.",
  "pm1.",
  "pm10c.",
  "pm18a.",
  "pm2.",
  "pm20.",
  "pm4.",
  "pm40.",
  "pmz.",
  // Tracker Packer
  "tp.",
  "tp1.",
  "tp2.",
  "tp3.",
  // SKYT / Startrekker Packer
  "skt.",
  "skyt.",
  "star.",
  "stpk.",
  // Heatseeker, ICE, Kefrens, Fuzac, Channel Players, Xann, PhaPacker, Zen
  "hrt.",
  "hrt!.",
  "ice.",
  "kef.",
  "kef7.",
  "fuz.",
  "fuzz.",
  "chan.",
  "xan.",
  "xann.",
  "pha.",
  "zen.",
  // UNIC
  "unic.",
  "unic2.",
  "un2.",
  // Other PTK-Prowiz variants
  "!pm!.",
  "ac1.",
  "ac1d.",
  "aval.",
  "cp.",
  "cplx.",
  "crb.",
  "di.",
  "eu.",
  "fc-m.",
  "fcm.",
  "ft.",
  "gv.",
  "hmc.",
  "it1.",
  "lax.",
  "mexxmp.",
  "mpro.",
  "nr.",
  "nru.",
  "ntpk.",
  "pin.",
  "polk.",
  "prom.",
  "pwr.",
  "pyg.",
  "pygm.",
  "pygmy.",
  "snt.",
  "snt!.",
  "st2.",
  "st26.",
  "st30.",
  "wn.",
  // SGT (Graoumf Tracker alias)
  "sgt.",
  // NTP1 (NewtronPacker variant)
  "ntp1."
];
const EXT_TO_UADE_PREFIX = {
  "ah": "ash",
  // Ashley Hogg → eagleplayer prefix "ash"
  "bvs": "bss",
  // Beathoven Synth v2 → "bss" (BeathovenSynthesizer)
  "cd": "core",
  // Core Design → "core"
  "chip": "kris",
  // ChipTracker → "kris"
  "dlw": "dl",
  // Dave Lowe WTD → "dl" (DaveLowe)
  "fp2": "fp",
  // Future Player 2 → "fp" (FuturePlayer)
  "fredmon": "fred",
  // Fred Editor Monitor → "fred"
  "mxt": "mxt",
  // MaxTrax (no eagleplayer — uses content detection)
  "nt": "mod",
  // NoiseTracker → "mod" (PTK-Prowiz)
  "ntsp": "two",
  // NTSP → "two" (NTSP-system)
  "psum": "snk",
  // Paul Summers → "snk"
  "rhst": "rho",
  // Rob Hubbard ST → "rho"
  "rkl": "rkl",
  // Ron Klaren (no eagleplayer entry — content detection)
  "sc2": "scn",
  // Sean Connolly 2 → "scn"
  "sil": "mok",
  // Silmarils v2 → "mok"
  "tomy": "sg",
  // TomyTracker → "sg"
  "tme": "tme",
  // TheMusicalEnlightenment → "tme"
  "unic": "unic",
  // UNIC Tracker → "unic" (in PTK-Prowiz list)
  "ins": "ins",
  // InStereo! → "ins"
  // PTK-Prowiz packed format extension aliases
  "p22a": "p21",
  // ProPacker 2.2a → p21 prefix
  "p30a": "p30",
  // ProPacker 3.0a → p30
  "pp": "ppk",
  // PowerPacker → ppk
  "pru2": "pru2",
  // ProRunner 2 → pru2
  "p40a": "p40a",
  // The Player 4.0a → p40a
  "smp": "pru1",
  // ProRunner .smp variant
  "ntp1": "ntp1"
  // NewtronPacker v1
};
async function tryUADEPrefixParse(buffer, filename, originalFileName, prefs, subsong, preScannedMeta) {
  const base = getBasename(filename);
  const ext = base.slice(base.lastIndexOf(".") + 1);
  const matchedPrefix = UADE_ONLY_PREFIXES.find((p) => base.startsWith(p) || p === `${ext}.`);
  if (matchedPrefix) {
    let uadeFileName = originalFileName;
    if (!base.startsWith(matchedPrefix)) {
      const dot = base.lastIndexOf(".");
      if (dot > 0) {
        const uadePrefix = EXT_TO_UADE_PREFIX[ext] || ext;
        const namePart = base.slice(0, dot);
        uadeFileName = `${uadePrefix}.${namePart}`;
      }
    }
    const { parseUADEFile: parseUADEFile2 } = await __vitePreload(async () => {
      const { parseUADEFile: parseUADEFile22 } = await import("./main-BbV5VyEH.js").then((n) => n.ji);
      return { parseUADEFile: parseUADEFile22 };
    }, true ? __vite__mapDeps([0,1,2,3,4,5,6]) : void 0);
    return parseUADEFile2(buffer, uadeFileName, prefs.uade ?? "enhanced", subsong, preScannedMeta);
  }
  const { isUADEFormat, parseUADEFile } = await __vitePreload(async () => {
    const { isUADEFormat: isUADEFormat2, parseUADEFile: parseUADEFile2 } = await import("./main-BbV5VyEH.js").then((n) => n.ji);
    return { isUADEFormat: isUADEFormat2, parseUADEFile: parseUADEFile2 };
  }, true ? __vite__mapDeps([0,1,2,3,4,5,6]) : void 0);
  if (isUADEFormat(filename)) {
    return await parseUADEFile(buffer, originalFileName, prefs.uade ?? "enhanced", subsong, preScannedMeta);
  }
  return null;
}
const OPENMPT_EXTENSIONS = /\.(s3m|it|mptm|xm|mod|m15|667|669|amf|ams|c67|cba|dbm|digi|dmf|dsm|dsym|dtm|etx|far|fmt|ftm|gdm|gmc|gt2|ice|imf|ims|itp|j2b|kris|mdl|med|mo3|mt2|mtm|mus|nru|nst|okt|plm|psm|pt36|ptm|puma|rtm|sfx|sfx2|ss|stk|stm|stp|stx|symmod|tcb|uax|ult|unic|wow|xmf)$/i;
async function tryPCTrackerParse(buffer, filename, originalFileName, _prefs, _subsong, _preScannedMeta) {
  if (OPENMPT_EXTENSIONS.test(filename)) {
    try {
      const { parseWithOpenMPT } = await __vitePreload(async () => {
        const { parseWithOpenMPT: parseWithOpenMPT2 } = await import("./OpenMPTConverter--7ALLBUZ.js");
        return { parseWithOpenMPT: parseWithOpenMPT2 };
      }, true ? __vite__mapDeps([21,0,1,2,3,4,5,6,22]) : void 0);
      return await parseWithOpenMPT(buffer, originalFileName);
    } catch (err) {
      console.warn(`[OpenMPT WASM] Parse failed for ${filename}, falling back to TS parser:`, err);
    }
  }
  if (/\.s3m$/i.test(filename)) {
    try {
      const { isS3MFormat, parseS3MFile } = await __vitePreload(async () => {
        const { isS3MFormat: isS3MFormat2, parseS3MFile: parseS3MFile2 } = await import("./S3MParser-DkDPrStE.js");
        return { isS3MFormat: isS3MFormat2, parseS3MFile: parseS3MFile2 };
      }, true ? __vite__mapDeps([23,0,1,2,3,4,5,6]) : void 0);
      if (isS3MFormat(buffer)) return parseS3MFile(buffer, originalFileName);
    } catch (err) {
      console.warn(`[S3MParser] Native parse failed for ${filename}, falling back to libopenmpt:`, err);
    }
  }
  if (/\.(it|mptm)$/i.test(filename)) {
    try {
      const { isITFormat, parseITFile } = await __vitePreload(async () => {
        const { isITFormat: isITFormat2, parseITFile: parseITFile2 } = await import("./ITParser-z_xBMeYv.js");
        return { isITFormat: isITFormat2, parseITFile: parseITFile2 };
      }, true ? __vite__mapDeps([24,0,1,2,3,4,5,6]) : void 0);
      if (isITFormat(buffer)) return parseITFile(buffer, originalFileName);
    } catch (err) {
      console.warn(`[ITParser] Native parse failed for ${filename}, falling back to libopenmpt:`, err);
    }
  }
  if (/\.xm$/i.test(filename)) {
    try {
      const { isXMFormat, parseXMFile } = await __vitePreload(async () => {
        const { isXMFormat: isXMFormat2, parseXMFile: parseXMFile2 } = await import("./main-BbV5VyEH.js").then((n) => n.jl);
        return { isXMFormat: isXMFormat2, parseXMFile: parseXMFile2 };
      }, true ? __vite__mapDeps([0,1,2,3,4,5,6]) : void 0);
      if (isXMFormat(buffer)) return await parseXMFile(buffer, originalFileName);
    } catch (err) {
      console.warn(`[XMParser] Native parse failed for ${filename}, falling back to libopenmpt:`, err);
    }
  }
  if (/\.(mod|m15)$/i.test(filename)) {
    try {
      const { isMODFormat, parseMODFile } = await __vitePreload(async () => {
        const { isMODFormat: isMODFormat2, parseMODFile: parseMODFile2 } = await import("./main-BbV5VyEH.js").then((n) => n.jm);
        return { isMODFormat: isMODFormat2, parseMODFile: parseMODFile2 };
      }, true ? __vite__mapDeps([0,1,2,3,4,5,6]) : void 0);
      if (isMODFormat(buffer)) return await parseMODFile(buffer, originalFileName);
    } catch (err) {
      console.warn(`[MODParser] Native parse failed for ${filename}, falling back to libopenmpt:`, err);
    }
  }
  return null;
}
function matchesExt(filename, exts) {
  const lower = filename.toLowerCase();
  const base = getBasename(lower);
  for (const ext of exts) {
    if (lower.endsWith("." + ext)) return true;
  }
  for (const ext of exts) {
    if (base.startsWith(ext + ".")) return true;
  }
  return false;
}
function toUADEPrefixName(filename, prefixes) {
  const base = getBasename(filename);
  const lower = base.toLowerCase();
  for (const pfx of prefixes) {
    if (lower.startsWith(pfx + ".")) return filename;
  }
  const lastDot = base.lastIndexOf(".");
  if (lastDot <= 0) return filename;
  const namePart = base.substring(0, lastDot);
  const extPart = base.substring(lastDot + 1);
  return `${extPart}.${namePart}`;
}
function isFCFormat(filename) {
  return matchesExt(filename, ["fc", "fc2", "fc3", "fc4", "fc13", "fc14", "sfc", "smod", "bfc", "bsi"]);
}
async function tryRouteFormat(buffer, filename, originalFileName, prefs, subsong, preScannedMeta, companionFiles) {
  var _a;
  const ctx = { buffer, originalFileName, prefs, subsong, preScannedMeta, companionFiles };
  if (filename.endsWith(".hvl") || filename.endsWith(".ahx")) {
    const { parseHivelyFile } = await __vitePreload(async () => {
      const { parseHivelyFile: parseHivelyFile2 } = await import("./main-BbV5VyEH.js").then((n) => n.jw);
      return { parseHivelyFile: parseHivelyFile2 };
    }, true ? __vite__mapDeps([0,1,2,3,4,5,6]) : void 0);
    return withNativeDefault("hvl", ctx, (buf, name) => parseHivelyFile(buf, name));
  }
  if (filename.endsWith(".kt") || filename.endsWith(".ki")) {
    const { parseKlystrack, isKlystrack } = await __vitePreload(async () => {
      const { parseKlystrack: parseKlystrack2, isKlystrack: isKlystrack2 } = await import("./KlysParser-D34hihxt.js");
      return { parseKlystrack: parseKlystrack2, isKlystrack: isKlystrack2 };
    }, true ? __vite__mapDeps([25,0,1,2,3,4,5,6]) : void 0);
    return withNativeDefault("kt", ctx, (buf, _name) => {
      const ab = buf instanceof ArrayBuffer ? buf : buf.buffer.slice(buf.byteOffset, buf.byteOffset + buf.byteLength);
      if (!isKlystrack(ab)) throw new Error("Not a valid klystrack file");
      return parseKlystrack(ab);
    });
  }
  if (filename.endsWith(".dmf")) {
    const bytes = new Uint8Array(buffer);
    if (prefs.xTracker === "native") {
      try {
        const { isXTrackerFormat, parseXTrackerFile } = await __vitePreload(async () => {
          const { isXTrackerFormat: isXTrackerFormat2, parseXTrackerFile: parseXTrackerFile2 } = await import("./XTrackerParser-BkoYrZTM.js");
          return { isXTrackerFormat: isXTrackerFormat2, parseXTrackerFile: parseXTrackerFile2 };
        }, true ? __vite__mapDeps([26,0,1,2,3,4,5,6]) : void 0);
        if (isXTrackerFormat(bytes)) {
          const result = parseXTrackerFile(bytes, originalFileName);
          if (result) return result;
        }
      } catch (err) {
        console.warn(`[XTrackerParser] Native parse failed for ${filename}, falling back:`, err);
      }
    }
    const isDefleMask = bytes.length > 16 && bytes[0] === 46 && bytes[1] === 68 && bytes[2] === 101 && bytes[3] === 70;
    const isDDMF = bytes[0] === 68 && bytes[1] === 68 && bytes[2] === 77 && bytes[3] === 70;
    if (isDefleMask) {
      const { parseDefleMaskToTrackerSong } = await __vitePreload(async () => {
        const { parseDefleMaskToTrackerSong: parseDefleMaskToTrackerSong2 } = await import("./DefleMaskToSong-Y7bxtjqc.js");
        return { parseDefleMaskToTrackerSong: parseDefleMaskToTrackerSong2 };
      }, true ? __vite__mapDeps([27,0,1,2,3,4,5,6]) : void 0);
      return parseDefleMaskToTrackerSong(buffer, originalFileName);
    }
    if (!isDDMF) {
      try {
        const { parseWithOpenMPT } = await __vitePreload(async () => {
          const { parseWithOpenMPT: parseWithOpenMPT2 } = await import("./OpenMPTConverter--7ALLBUZ.js");
          return { parseWithOpenMPT: parseWithOpenMPT2 };
        }, true ? __vite__mapDeps([21,0,1,2,3,4,5,6,22]) : void 0);
        return await parseWithOpenMPT(buffer, originalFileName);
      } catch {
        const { parseFurnaceFile } = await __vitePreload(async () => {
          const { parseFurnaceFile: parseFurnaceFile2 } = await import("./FurnaceToSong-MoKRY9wG.js");
          return { parseFurnaceFile: parseFurnaceFile2 };
        }, true ? __vite__mapDeps([28,0,1,2,3,4,5,6]) : void 0);
        return parseFurnaceFile(buffer, originalFileName, subsong);
      }
    }
    try {
      const { parseWithOpenMPT } = await __vitePreload(async () => {
        const { parseWithOpenMPT: parseWithOpenMPT2 } = await import("./OpenMPTConverter--7ALLBUZ.js");
        return { parseWithOpenMPT: parseWithOpenMPT2 };
      }, true ? __vite__mapDeps([21,0,1,2,3,4,5,6,22]) : void 0);
      return await parseWithOpenMPT(buffer, originalFileName);
    } catch {
      const { parseUADEFile: parseUADE_dmf } = await __vitePreload(async () => {
        const { parseUADEFile: parseUADE_dmf2 } = await import("./main-BbV5VyEH.js").then((n) => n.ji);
        return { parseUADEFile: parseUADE_dmf2 };
      }, true ? __vite__mapDeps([0,1,2,3,4,5,6]) : void 0);
      return parseUADE_dmf(buffer, originalFileName, prefs.uade ?? "enhanced", subsong, preScannedMeta);
    }
  }
  if (filename.endsWith(".fur")) {
    {
      const { parseFurnaceFile } = await __vitePreload(async () => {
        const { parseFurnaceFile: parseFurnaceFile2 } = await import("./FurnaceToSong-MoKRY9wG.js");
        return { parseFurnaceFile: parseFurnaceFile2 };
      }, true ? __vite__mapDeps([28,0,1,2,3,4,5,6]) : void 0);
      return parseFurnaceFile(buffer, originalFileName, subsong);
    }
  }
  if (filename.endsWith(".okt") || filename.endsWith(".okta")) {
    const { parseOktalyzerFile } = await __vitePreload(async () => {
      const { parseOktalyzerFile: parseOktalyzerFile2 } = await import("./OktalyzerParser-CvKeZnZ9.js");
      return { parseOktalyzerFile: parseOktalyzerFile2 };
    }, true ? __vite__mapDeps([29,0,1,2,3,4,5,6]) : void 0);
    return withNativeDefault("okt", ctx, (buf, name) => parseOktalyzerFile(buf, name));
  }
  {
    const buf4 = new Uint8Array(buffer instanceof ArrayBuffer ? buffer : buffer).subarray(0, 4);
    const isMEDExt = filename.endsWith(".med") || filename.endsWith(".mmd0") || filename.endsWith(".mmd1") || filename.endsWith(".mmd2") || filename.endsWith(".mmd3");
    const isMEDMagic = buf4[0] === 77 && buf4[1] === 77 && buf4[2] === 68 && (buf4[3] === 48 || buf4[3] === 49 || buf4[3] === 50 || buf4[3] === 51);
    if (isMEDExt || isMEDMagic) {
      const { parseMEDFile } = await __vitePreload(async () => {
        const { parseMEDFile: parseMEDFile2 } = await import("./MEDParser-DKJWsJvQ.js");
        return { parseMEDFile: parseMEDFile2 };
      }, true ? __vite__mapDeps([30,0,1,2,3,4,5,6]) : void 0);
      return withNativeDefault("med", ctx, (buf, name) => parseMEDFile(buf, name));
    }
  }
  if (filename.endsWith(".digi")) {
    if (ctx.prefs.digi !== "uade") {
      const { parseDigiBoosterFile } = await __vitePreload(async () => {
        const { parseDigiBoosterFile: parseDigiBoosterFile2 } = await import("./DigiBoosterParser-D-3oOuS8.js");
        return { parseDigiBoosterFile: parseDigiBoosterFile2 };
      }, true ? __vite__mapDeps([31,0,1,2,3,4,5,6]) : void 0);
      try {
        const song = parseDigiBoosterFile(ctx.buffer, ctx.originalFileName);
        song.libopenmptFileData = buffer.slice(0);
        return song;
      } catch {
        const { parseWithOpenMPT } = await __vitePreload(async () => {
          const { parseWithOpenMPT: parseWithOpenMPT2 } = await import("./OpenMPTConverter--7ALLBUZ.js");
          return { parseWithOpenMPT: parseWithOpenMPT2 };
        }, true ? __vite__mapDeps([21,0,1,2,3,4,5,6,22]) : void 0);
        const song = await parseWithOpenMPT(buffer, originalFileName);
        song.libopenmptFileData = buffer.slice(0);
        return song;
      }
    }
    const { parseUADEFile } = await __vitePreload(async () => {
      const { parseUADEFile: parseUADEFile2 } = await import("./main-BbV5VyEH.js").then((n) => n.ji);
      return { parseUADEFile: parseUADEFile2 };
    }, true ? __vite__mapDeps([0,1,2,3,4,5,6]) : void 0);
    return parseUADEFile(ctx.buffer, ctx.originalFileName, ctx.prefs.uade ?? "enhanced", ctx.subsong, ctx.preScannedMeta);
  }
  if (filename.endsWith(".dm2")) {
    const { isDeltaMusic2Format, parseDeltaMusic2File } = await __vitePreload(async () => {
      const { isDeltaMusic2Format: isDeltaMusic2Format2, parseDeltaMusic2File: parseDeltaMusic2File2 } = await import("./DeltaMusic2Parser-BNBAj9qa.js");
      return { isDeltaMusic2Format: isDeltaMusic2Format2, parseDeltaMusic2File: parseDeltaMusic2File2 };
    }, true ? __vite__mapDeps([32,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "deltaMusic2",
      ctx,
      (bytes, name) => parseDeltaMusic2File(bytes, name),
      "DeltaMusic2Parser",
      { isFormat: isDeltaMusic2Format, usesBytes: true, injectUADE: true }
    );
  }
  if (isFCFormat(filename)) {
    const { parseFCFile } = await __vitePreload(async () => {
      const { parseFCFile: parseFCFile2 } = await import("./FCParser-DLdFz_2C.js");
      return { parseFCFile: parseFCFile2 };
    }, true ? __vite__mapDeps([33,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE("fc", ctx, (buf, name) => parseFCFile(buf, name), "FCParser", { injectUADE: true });
  }
  if (matchesExt(filename, ["bp", "bp3", "sndmon"])) {
    const { parseSoundMonFile } = await __vitePreload(async () => {
      const { parseSoundMonFile: parseSoundMonFile2 } = await import("./SoundMonParser-uW5D_Bww.js");
      return { parseSoundMonFile: parseSoundMonFile2 };
    }, true ? __vite__mapDeps([34,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE("soundmon", ctx, (buf, name) => parseSoundMonFile(buf, name), "SoundMonParser", { injectUADE: true });
  }
  if (matchesExt(filename, ["smn"])) {
    if (prefs.sidmon1 !== "uade") {
      try {
        const { isSidMon1Format, parseSidMon1File } = await __vitePreload(async () => {
          const { isSidMon1Format: isSidMon1Format2, parseSidMon1File: parseSidMon1File2 } = await import("./SidMon1Parser-C0NDTL2s.js");
          return { isSidMon1Format: isSidMon1Format2, parseSidMon1File: parseSidMon1File2 };
        }, true ? __vite__mapDeps([35,0,1,2,3,4,5,6]) : void 0);
        if (isSidMon1Format(buffer)) {
          const result = parseSidMon1File(buffer, originalFileName);
          result.uadeEditableFileData = buffer.slice(0);
          result.uadeEditableFileName = originalFileName;
          return result;
        }
      } catch (err) {
        console.warn(`[SidMon1Parser] Native parse failed for ${filename}, falling back:`, err);
      }
    }
    {
      const { parseSidMon2File } = await __vitePreload(async () => {
        const { parseSidMon2File: parseSidMon2File2 } = await import("./SidMon2Parser-C0Bch-oo.js");
        return { parseSidMon2File: parseSidMon2File2 };
      }, true ? __vite__mapDeps([36,0,1,2,3,4,5,6]) : void 0);
      return parseSidMon2File(buffer, originalFileName);
    }
  }
  if (matchesExt(filename, ["sid2", "sd2"])) {
    const { parseSidMon2File } = await __vitePreload(async () => {
      const { parseSidMon2File: parseSidMon2File2 } = await import("./SidMon2Parser-C0Bch-oo.js");
      return { parseSidMon2File: parseSidMon2File2 };
    }, true ? __vite__mapDeps([36,0,1,2,3,4,5,6]) : void 0);
    return parseSidMon2File(buffer, originalFileName);
  }
  if (matchesExt(filename, ["fred"])) {
    const { isFredEditorFormat, parseFredEditorFile } = await __vitePreload(async () => {
      const { isFredEditorFormat: isFredEditorFormat2, parseFredEditorFile: parseFredEditorFile2 } = await import("./FredEditorParser-CSrVkxu_.js");
      return { isFredEditorFormat: isFredEditorFormat2, parseFredEditorFile: parseFredEditorFile2 };
    }, true ? __vite__mapDeps([37,0,1,2,3,4,5,6,38]) : void 0);
    return withNativeThenUADE(
      "fred",
      ctx,
      (buf, name) => {
        const ab = buf instanceof Uint8Array ? buf.buffer : buf;
        if (isFredEditorFormat(ab)) return parseFredEditorFile(ab, name);
        return null;
      },
      "FredEditorParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["sfx", "sfx2"])) {
    const { parseWithOpenMPT } = await __vitePreload(async () => {
      const { parseWithOpenMPT: parseWithOpenMPT2 } = await import("./OpenMPTConverter--7ALLBUZ.js");
      return { parseWithOpenMPT: parseWithOpenMPT2 };
    }, true ? __vite__mapDeps([21,0,1,2,3,4,5,6,22]) : void 0);
    const song = await parseWithOpenMPT(buffer, originalFileName);
    song.libopenmptFileData = buffer.slice(0);
    return song;
  }
  if (matchesExt(filename, ["sfx13"])) {
    const { isSoundFXFormat, parseSoundFXFile } = await __vitePreload(async () => {
      const { isSoundFXFormat: isSoundFXFormat2, parseSoundFXFile: parseSoundFXFile2 } = await import("./SoundFXParser-Dq4k3u4G.js");
      return { isSoundFXFormat: isSoundFXFormat2, parseSoundFXFile: parseSoundFXFile2 };
    }, true ? __vite__mapDeps([39,40,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "soundfx",
      ctx,
      (buf, name) => {
        const ab = buf instanceof Uint8Array ? buf.buffer : buf;
        if (isSoundFXFormat(ab)) return parseSoundFXFile(ab, name);
        return null;
      },
      "SoundFXParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["jam", "jc"])) {
    try {
      const { isJamCrackerFormat, parseJamCrackerFile } = await __vitePreload(async () => {
        const { isJamCrackerFormat: isJamCrackerFormat2, parseJamCrackerFile: parseJamCrackerFile2 } = await import("./JamCrackerParser-BM0lewvd.js");
        return { isJamCrackerFormat: isJamCrackerFormat2, parseJamCrackerFile: parseJamCrackerFile2 };
      }, true ? __vite__mapDeps([41,0,1,2,3,4,5,6]) : void 0);
      if (isJamCrackerFormat(buffer)) {
        return parseJamCrackerFile(buffer, originalFileName);
      }
    } catch (err) {
      console.warn(`[JamCrackerParser] Native parse failed for ${filename}, falling back to UADE:`, err);
    }
    const { parseUADEFile } = await __vitePreload(async () => {
      const { parseUADEFile: parseUADEFile2 } = await import("./main-BbV5VyEH.js").then((n) => n.ji);
      return { parseUADEFile: parseUADEFile2 };
    }, true ? __vite__mapDeps([0,1,2,3,4,5,6]) : void 0);
    return parseUADEFile(buffer, originalFileName, prefs.uade ?? "enhanced", subsong, preScannedMeta);
  }
  if (matchesExt(filename, ["emod", "qc"])) {
    try {
      const { isQuadraComposerFormat, parseQuadraComposerFile } = await __vitePreload(async () => {
        const { isQuadraComposerFormat: isQuadraComposerFormat2, parseQuadraComposerFile: parseQuadraComposerFile2 } = await import("./QuadraComposerParser-Dz3Cm10H.js");
        return { isQuadraComposerFormat: isQuadraComposerFormat2, parseQuadraComposerFile: parseQuadraComposerFile2 };
      }, true ? __vite__mapDeps([42,43,0,1,2,3,4,5,6]) : void 0);
      if (isQuadraComposerFormat(buffer)) {
        return injectUADEPlayback(await parseQuadraComposerFile(buffer, originalFileName), ctx);
      }
    } catch (err) {
      console.warn(`[QuadraComposerParser] Native parse failed for ${filename}, falling back to UADE:`, err);
    }
    return callUADE(ctx);
  }
  if (matchesExt(filename, ["abk"])) {
    try {
      const { isAMOSMusicBankFormat, parseAMOSMusicBankFile } = await __vitePreload(async () => {
        const { isAMOSMusicBankFormat: isAMOSMusicBankFormat2, parseAMOSMusicBankFile: parseAMOSMusicBankFile2 } = await import("./AMOSMusicBankParser-BeX5Hrg-.js");
        return { isAMOSMusicBankFormat: isAMOSMusicBankFormat2, parseAMOSMusicBankFile: parseAMOSMusicBankFile2 };
      }, true ? __vite__mapDeps([44,0,1,2,3,4,5,6]) : void 0);
      if (isAMOSMusicBankFormat(buffer)) {
        return parseAMOSMusicBankFile(buffer, originalFileName);
      }
    } catch (err) {
      console.warn(`[AMOSMusicBankParser] Native parse failed for ${filename}, falling back to UADE:`, err);
    }
    const { parseUADEFile } = await __vitePreload(async () => {
      const { parseUADEFile: parseUADEFile2 } = await import("./main-BbV5VyEH.js").then((n) => n.ji);
      return { parseUADEFile: parseUADEFile2 };
    }, true ? __vite__mapDeps([0,1,2,3,4,5,6]) : void 0);
    return parseUADEFile(buffer, originalFileName, prefs.uade ?? "enhanced", subsong, preScannedMeta);
  }
  if (matchesExt(filename, ["sa", "sonic"])) {
    const { isSonicArrangerFormat, parseSonicArrangerFile } = await __vitePreload(async () => {
      const { isSonicArrangerFormat: isSonicArrangerFormat2, parseSonicArrangerFile: parseSonicArrangerFile2 } = await import("./SonicArrangerParser-BntgX101.js");
      return { isSonicArrangerFormat: isSonicArrangerFormat2, parseSonicArrangerFile: parseSonicArrangerFile2 };
    }, true ? __vite__mapDeps([45,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "sonicArranger",
      ctx,
      (buf, name) => {
        if (isSonicArrangerFormat(buf)) return parseSonicArrangerFile(buf, name) ?? null;
        return null;
      },
      "SonicArrangerParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["sas"])) {
    const { isSonicArrangerFormat, parseSonicArrangerFile } = await __vitePreload(async () => {
      const { isSonicArrangerFormat: isSonicArrangerFormat2, parseSonicArrangerFile: parseSonicArrangerFile2 } = await import("./SonicArrangerParser-BntgX101.js");
      return { isSonicArrangerFormat: isSonicArrangerFormat2, parseSonicArrangerFile: parseSonicArrangerFile2 };
    }, true ? __vite__mapDeps([45,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "sonicArranger",
      ctx,
      (buf, name) => {
        const ab = buf instanceof Uint8Array ? buf.buffer : buf;
        if (isSonicArrangerFormat(ab)) return parseSonicArrangerFile(ab, name);
        return null;
      },
      "SonicArrangerParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["is20"])) {
    try {
      const { isInStereo2Format, parseInStereo2File } = await __vitePreload(async () => {
        const { isInStereo2Format: isInStereo2Format2, parseInStereo2File: parseInStereo2File2 } = await import("./InStereo2Parser-DMckRwPw.js");
        return { isInStereo2Format: isInStereo2Format2, parseInStereo2File: parseInStereo2File2 };
      }, true ? __vite__mapDeps([46,0,1,2,3,4,5,6]) : void 0);
      const bytes = new Uint8Array(buffer);
      if (isInStereo2Format(bytes)) {
        const result = parseInStereo2File(bytes, originalFileName);
        if (result) {
          result.uadeEditableFileData = buffer.slice(0);
          result.uadeEditableFileName = originalFileName;
          return result;
        }
      }
    } catch (err) {
      console.warn(`[InStereo2Parser] Native parse failed for ${filename}, falling back to UADE:`, err);
    }
    const { parseUADEFile } = await __vitePreload(async () => {
      const { parseUADEFile: parseUADEFile2 } = await import("./main-BbV5VyEH.js").then((n) => n.ji);
      return { parseUADEFile: parseUADEFile2 };
    }, true ? __vite__mapDeps([0,1,2,3,4,5,6]) : void 0);
    return parseUADEFile(buffer, originalFileName, "classic", subsong, preScannedMeta);
  }
  if (matchesExt(filename, ["is10"])) {
    try {
      const { isInStereo1Format, parseInStereo1File } = await __vitePreload(async () => {
        const { isInStereo1Format: isInStereo1Format2, parseInStereo1File: parseInStereo1File2 } = await import("./InStereo1Parser-Db-e2aBz.js");
        return { isInStereo1Format: isInStereo1Format2, parseInStereo1File: parseInStereo1File2 };
      }, true ? __vite__mapDeps([47,0,1,2,3,4,5,6]) : void 0);
      const bytes = new Uint8Array(buffer);
      if (isInStereo1Format(bytes)) {
        const result = parseInStereo1File(bytes, originalFileName);
        if (result) {
          result.uadeEditableFileData = buffer.slice(0);
          result.uadeEditableFileName = originalFileName;
          return result;
        }
      }
    } catch (err) {
      console.warn(`[InStereo1Parser] Native parse failed for ${filename}, falling back to UADE:`, err);
    }
    const { parseUADEFile } = await __vitePreload(async () => {
      const { parseUADEFile: parseUADEFile2 } = await import("./main-BbV5VyEH.js").then((n) => n.ji);
      return { parseUADEFile: parseUADEFile2 };
    }, true ? __vite__mapDeps([0,1,2,3,4,5,6]) : void 0);
    return parseUADEFile(buffer, originalFileName, "classic", subsong, preScannedMeta);
  }
  if (matchesExt(filename, ["is"])) {
    const bytes = new Uint8Array(buffer);
    try {
      const { isInStereo1Format, parseInStereo1File } = await __vitePreload(async () => {
        const { isInStereo1Format: isInStereo1Format2, parseInStereo1File: parseInStereo1File2 } = await import("./InStereo1Parser-Db-e2aBz.js");
        return { isInStereo1Format: isInStereo1Format2, parseInStereo1File: parseInStereo1File2 };
      }, true ? __vite__mapDeps([47,0,1,2,3,4,5,6]) : void 0);
      if (isInStereo1Format(bytes)) {
        const result = parseInStereo1File(bytes, originalFileName);
        if (result) {
          result.uadeEditableFileData = buffer.slice(0);
          result.uadeEditableFileName = originalFileName;
          return result;
        }
      }
    } catch (err) {
      console.warn(`[InStereo1Parser] Native parse failed for ${filename}, trying IS2:`, err);
    }
    try {
      const { isInStereo2Format, parseInStereo2File } = await __vitePreload(async () => {
        const { isInStereo2Format: isInStereo2Format2, parseInStereo2File: parseInStereo2File2 } = await import("./InStereo2Parser-DMckRwPw.js");
        return { isInStereo2Format: isInStereo2Format2, parseInStereo2File: parseInStereo2File2 };
      }, true ? __vite__mapDeps([46,0,1,2,3,4,5,6]) : void 0);
      if (isInStereo2Format(bytes)) {
        const result = parseInStereo2File(bytes, originalFileName);
        if (result) {
          result.uadeEditableFileData = buffer.slice(0);
          result.uadeEditableFileName = originalFileName;
          return result;
        }
      }
    } catch (err) {
      console.warn(`[InStereo2Parser] Native parse failed for ${filename}, falling back to UADE:`, err);
    }
    const { parseUADEFile } = await __vitePreload(async () => {
      const { parseUADEFile: parseUADEFile2 } = await import("./main-BbV5VyEH.js").then((n) => n.ji);
      return { parseUADEFile: parseUADEFile2 };
    }, true ? __vite__mapDeps([0,1,2,3,4,5,6]) : void 0);
    return parseUADEFile(buffer, originalFileName, "classic", subsong, preScannedMeta);
  }
  if (matchesExt(filename, ["prt"])) {
    const { parseUADEFile } = await __vitePreload(async () => {
      const { parseUADEFile: parseUADEFile2 } = await import("./main-BbV5VyEH.js").then((n) => n.ji);
      return { parseUADEFile: parseUADEFile2 };
    }, true ? __vite__mapDeps([0,1,2,3,4,5,6]) : void 0);
    return parseUADEFile(buffer, originalFileName, prefs.uade ?? "enhanced", subsong, preScannedMeta);
  }
  if (matchesExt(filename, ["hipc", "soc", "coso"])) {
    if (prefs.hippelCoso !== "uade") {
      try {
        const { isHippelCoSoFormat, parseHippelCoSoFile } = await __vitePreload(async () => {
          const { isHippelCoSoFormat: isHippelCoSoFormat2, parseHippelCoSoFile: parseHippelCoSoFile2 } = await import("./HippelCoSoParser-DydFFz-Y.js");
          return { isHippelCoSoFormat: isHippelCoSoFormat2, parseHippelCoSoFile: parseHippelCoSoFile2 };
        }, true ? __vite__mapDeps([48,0,1,2,3,4,5,6]) : void 0);
        if (isHippelCoSoFormat(buffer)) {
          return parseHippelCoSoFile(buffer, originalFileName);
        }
      } catch (err) {
        console.warn(`[HippelCoSoParser] Native parse failed for ${filename}, falling back to UADE:`, err);
      }
    }
    const { parseUADEFile } = await __vitePreload(async () => {
      const { parseUADEFile: parseUADEFile2 } = await import("./main-BbV5VyEH.js").then((n) => n.ji);
      return { parseUADEFile: parseUADEFile2 };
    }, true ? __vite__mapDeps([0,1,2,3,4,5,6]) : void 0);
    return parseUADEFile(buffer, originalFileName, prefs.uade ?? "enhanced", subsong, preScannedMeta);
  }
  if (matchesExt(filename, ["rh", "rhp"])) {
    const { isRobHubbardFormat, parseRobHubbardFile } = await __vitePreload(async () => {
      const { isRobHubbardFormat: isRobHubbardFormat2, parseRobHubbardFile: parseRobHubbardFile2 } = await import("./RobHubbardParser-DczqYvyl.js");
      return { isRobHubbardFormat: isRobHubbardFormat2, parseRobHubbardFile: parseRobHubbardFile2 };
    }, true ? __vite__mapDeps([49,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "robHubbard",
      ctx,
      (buf, name) => {
        const ab = buf instanceof Uint8Array ? buf.buffer : buf;
        if (isRobHubbardFormat(ab, name)) return parseRobHubbardFile(ab, name);
        return null;
      },
      "RobHubbardParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["tfmx", "mdat", "tfx"])) {
    try {
      const { parseTFMXFile } = await __vitePreload(async () => {
        const { parseTFMXFile: parseTFMXFile2 } = await import("./TFMXParser-DNc9g4Yy.js");
        return { parseTFMXFile: parseTFMXFile2 };
      }, true ? __vite__mapDeps([50,51,0,1,2,3,4,5,6]) : void 0);
      const nativeSong = parseTFMXFile(buffer, originalFileName, subsong);
      let smplData;
      if (companionFiles) {
        for (const [key, val] of companionFiles) {
          const base = ((_a = key.split("/").pop()) == null ? void 0 : _a.toLowerCase()) ?? "";
          if (base.startsWith("smpl.")) {
            smplData = val;
            break;
          }
        }
      }
      return {
        ...nativeSong,
        format: "TFMX",
        // tfmxFileData triggers TFMXEngine WASM module playback (suppressNotes=true)
        tfmxFileData: buffer.slice(0),
        tfmxSmplData: smplData == null ? void 0 : smplData.slice(0)
      };
    } catch (err) {
      console.warn(`[TFMX] Native parse failed, falling back to UADE:`, err);
      const { parseUADEFile } = await __vitePreload(async () => {
        const { parseUADEFile: parseUADEFile2 } = await import("./main-BbV5VyEH.js").then((n) => n.ji);
        return { parseUADEFile: parseUADEFile2 };
      }, true ? __vite__mapDeps([0,1,2,3,4,5,6]) : void 0);
      return await parseUADEFile(buffer, originalFileName, prefs.uade ?? "enhanced", subsong, preScannedMeta, companionFiles);
    }
  }
  if (matchesExt(filename, ["dmu", "dmu2", "mug", "mug2"])) {
    const { parseDigitalMugicianFile } = await __vitePreload(async () => {
      const { parseDigitalMugicianFile: parseDigitalMugicianFile2 } = await import("./DigitalMugicianParser-DlVBZ5dm.js");
      return { parseDigitalMugicianFile: parseDigitalMugicianFile2 };
    }, true ? __vite__mapDeps([52,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE("mugician", ctx, (buf, name) => parseDigitalMugicianFile(buf, name), "DigitalMugicianParser", { injectUADE: true });
  }
  {
    const chipResult = await tryChipDumpParse(buffer, filename, originalFileName);
    if (chipResult) return chipResult;
  }
  if (matchesExt(filename, ["sf2"])) {
    const { isSIDFactory2File, parseSIDFactory2File } = await __vitePreload(async () => {
      const { isSIDFactory2File: isSIDFactory2File2, parseSIDFactory2File: parseSIDFactory2File2 } = await import("./SIDFactory2Parser-B3Y3ky55.js");
      return { isSIDFactory2File: isSIDFactory2File2, parseSIDFactory2File: parseSIDFactory2File2 };
    }, true ? [] : void 0);
    if (isSIDFactory2File(buffer)) {
      return parseSIDFactory2File(buffer, originalFileName);
    }
    throw new Error(`${originalFileName}: SoundFont (.sf2) files are not supported as tracker formats`);
  }
  if (matchesExt(filename, ["sid1"])) {
    if (prefs.sidmon1 !== "uade") {
      try {
        const { isSidMon1Format, parseSidMon1File } = await __vitePreload(async () => {
          const { isSidMon1Format: isSidMon1Format2, parseSidMon1File: parseSidMon1File2 } = await import("./SidMon1Parser-C0NDTL2s.js");
          return { isSidMon1Format: isSidMon1Format2, parseSidMon1File: parseSidMon1File2 };
        }, true ? __vite__mapDeps([35,0,1,2,3,4,5,6]) : void 0);
        if (isSidMon1Format(buffer)) {
          const result = parseSidMon1File(buffer, originalFileName);
          result.uadeEditableFileData = buffer.slice(0);
          result.uadeEditableFileName = originalFileName;
          return result;
        }
      } catch (err) {
        console.warn(`[SidMon1Parser] Native parse failed for ${filename}, falling back to SID:`, err);
      }
    }
    const { parseSIDFile } = await __vitePreload(async () => {
      const { parseSIDFile: parseSIDFile2 } = await import("./SIDParser-7jTsMEg5.js");
      return { parseSIDFile: parseSIDFile2 };
    }, true ? __vite__mapDeps([53,10]) : void 0);
    return parseSIDFile(buffer, originalFileName);
  }
  if (matchesExt(filename, ["sid"])) {
    const { isSIDFormat, parseSIDFile } = await __vitePreload(async () => {
      const { isSIDFormat: isSIDFormat2, parseSIDFile: parseSIDFile2 } = await import("./SIDParser-7jTsMEg5.js");
      return { isSIDFormat: isSIDFormat2, parseSIDFile: parseSIDFile2 };
    }, true ? __vite__mapDeps([53,10]) : void 0);
    if (isSIDFormat(buffer)) {
      return parseSIDFile(buffer, originalFileName);
    }
    if (prefs.sidmon1 !== "uade") {
      try {
        const { isSidMon1Format, parseSidMon1File } = await __vitePreload(async () => {
          const { isSidMon1Format: isSidMon1Format2, parseSidMon1File: parseSidMon1File2 } = await import("./SidMon1Parser-C0NDTL2s.js");
          return { isSidMon1Format: isSidMon1Format2, parseSidMon1File: parseSidMon1File2 };
        }, true ? __vite__mapDeps([35,0,1,2,3,4,5,6]) : void 0);
        if (isSidMon1Format(buffer)) {
          const result = parseSidMon1File(buffer, originalFileName);
          result.uadeEditableFileData = buffer.slice(0);
          result.uadeEditableFileName = originalFileName;
          return result;
        }
      } catch (err) {
        console.warn(`[SidMon1Parser] Native parse failed for ${filename}, falling back to UADE:`, err);
      }
    }
    const { parseUADEFile } = await __vitePreload(async () => {
      const { parseUADEFile: parseUADEFile2 } = await import("./main-BbV5VyEH.js").then((n) => n.ji);
      return { parseUADEFile: parseUADEFile2 };
    }, true ? __vite__mapDeps([0,1,2,3,4,5,6]) : void 0);
    return parseUADEFile(buffer, originalFileName, prefs.uade ?? "enhanced", subsong, preScannedMeta);
  }
  if (matchesExt(filename, ["dw", "dwold"])) {
    if (prefs.davidWhittaker !== "uade") {
      try {
        const { parseDavidWhittakerFile } = await __vitePreload(async () => {
          const { parseDavidWhittakerFile: parseDavidWhittakerFile2 } = await import("./DavidWhittakerParser-BhHjuDNq.js");
          return { parseDavidWhittakerFile: parseDavidWhittakerFile2 };
        }, true ? __vite__mapDeps([54,0,1,2,3,4,5,6]) : void 0);
        const dwResult = parseDavidWhittakerFile(buffer, originalFileName);
        const dwNotes = dwResult.patterns.reduce((sum, p) => sum + p.channels.reduce((cs, ch) => cs + ch.rows.filter((r) => r.note > 0).length, 0), 0);
        if (dwNotes > 0) return dwResult;
        console.warn(`[DavidWhittakerParser] 0 notes extracted, falling back to UADE`);
      } catch (err) {
        console.warn(`[DavidWhittakerParser] Native parse failed for ${filename}, falling back to UADE:`, err);
      }
    }
    const { parseUADEFile: parseUADE } = await __vitePreload(async () => {
      const { parseUADEFile: parseUADE2 } = await import("./main-BbV5VyEH.js").then((n) => n.ji);
      return { parseUADEFile: parseUADE2 };
    }, true ? __vite__mapDeps([0,1,2,3,4,5,6]) : void 0);
    return parseUADE(buffer, originalFileName, prefs.uade ?? "enhanced", subsong, preScannedMeta);
  }
  if (matchesExt(filename, ["aon", "aon8"])) {
    const { isArtOfNoiseFormat, parseArtOfNoiseFile } = await __vitePreload(async () => {
      const { isArtOfNoiseFormat: isArtOfNoiseFormat2, parseArtOfNoiseFile: parseArtOfNoiseFile2 } = await import("./ArtOfNoiseParser-4YM8QbJl.js");
      return { isArtOfNoiseFormat: isArtOfNoiseFormat2, parseArtOfNoiseFile: parseArtOfNoiseFile2 };
    }, true ? __vite__mapDeps([55,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "artOfNoise",
      ctx,
      (bytes, name) => parseArtOfNoiseFile(bytes, name),
      "ArtOfNoiseParser",
      { isFormat: isArtOfNoiseFormat, usesBytes: true, injectUADE: true }
    );
  }
  if (matchesExt(filename, ["dsym"])) {
    const { parseWithOpenMPT } = await __vitePreload(async () => {
      const { parseWithOpenMPT: parseWithOpenMPT2 } = await import("./OpenMPTConverter--7ALLBUZ.js");
      return { parseWithOpenMPT: parseWithOpenMPT2 };
    }, true ? __vite__mapDeps([21,0,1,2,3,4,5,6,22]) : void 0);
    const song = await parseWithOpenMPT(buffer, originalFileName);
    song.libopenmptFileData = buffer.slice(0);
    return song;
  }
  if (matchesExt(filename, ["gt2"])) {
    const { parseWithOpenMPT } = await __vitePreload(async () => {
      const { parseWithOpenMPT: parseWithOpenMPT2 } = await import("./OpenMPTConverter--7ALLBUZ.js");
      return { parseWithOpenMPT: parseWithOpenMPT2 };
    }, true ? __vite__mapDeps([21,0,1,2,3,4,5,6,22]) : void 0);
    const song = await parseWithOpenMPT(buffer, originalFileName);
    song.libopenmptFileData = buffer.slice(0);
    return song;
  }
  if (matchesExt(filename, ["gtk"])) {
    const { isGraoumfTracker2Format, parseGraoumfTracker2File } = await __vitePreload(async () => {
      const { isGraoumfTracker2Format: isGraoumfTracker2Format2, parseGraoumfTracker2File: parseGraoumfTracker2File2 } = await import("./GraoumfTracker2Parser-kkNVCBx4.js");
      return { isGraoumfTracker2Format: isGraoumfTracker2Format2, parseGraoumfTracker2File: parseGraoumfTracker2File2 };
    }, true ? __vite__mapDeps([56,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "graoumfTracker2",
      ctx,
      (buf, name) => {
        const u8 = buf instanceof Uint8Array ? buf : new Uint8Array(buf);
        if (isGraoumfTracker2Format(u8)) return parseGraoumfTracker2File(u8, name);
        return null;
      },
      "GraoumfTracker2Parser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["symmod"])) {
    const { parseWithOpenMPT } = await __vitePreload(async () => {
      const { parseWithOpenMPT: parseWithOpenMPT2 } = await import("./OpenMPTConverter--7ALLBUZ.js");
      return { parseWithOpenMPT: parseWithOpenMPT2 };
    }, true ? __vite__mapDeps([21,0,1,2,3,4,5,6,22]) : void 0);
    const song = await parseWithOpenMPT(buffer, originalFileName);
    song.libopenmptFileData = buffer.slice(0);
    for (const inst of song.instruments) {
      const raw = inst.name ?? "";
      const slashIdx = raw.lastIndexOf("/");
      const colonIdx = raw.lastIndexOf(":");
      const stripIdx = Math.max(slashIdx, colonIdx);
      if (stripIdx >= 0) inst.name = raw.substring(stripIdx + 1);
    }
    return song;
  }
  if (matchesExt(filename, ["dbm"])) {
    const { parseWithOpenMPT } = await __vitePreload(async () => {
      const { parseWithOpenMPT: parseWithOpenMPT2 } = await import("./OpenMPTConverter--7ALLBUZ.js");
      return { parseWithOpenMPT: parseWithOpenMPT2 };
    }, true ? __vite__mapDeps([21,0,1,2,3,4,5,6,22]) : void 0);
    const song = await parseWithOpenMPT(buffer, originalFileName);
    song.libopenmptFileData = buffer.slice(0);
    return song;
  }
  if (matchesExt(filename, ["puma"])) {
    try {
      const { parseWithOpenMPT } = await __vitePreload(async () => {
        const { parseWithOpenMPT: parseWithOpenMPT2 } = await import("./OpenMPTConverter--7ALLBUZ.js");
        return { parseWithOpenMPT: parseWithOpenMPT2 };
      }, true ? __vite__mapDeps([21,0,1,2,3,4,5,6,22]) : void 0);
      return await parseWithOpenMPT(buffer, originalFileName);
    } catch {
      const { isPumaTrackerFormat, parsePumaTrackerFile } = await __vitePreload(async () => {
        const { isPumaTrackerFormat: isPumaTrackerFormat2, parsePumaTrackerFile: parsePumaTrackerFile2 } = await import("./PumaTrackerParser-Bhgp5dgm.js");
        return { isPumaTrackerFormat: isPumaTrackerFormat2, parsePumaTrackerFile: parsePumaTrackerFile2 };
      }, true ? __vite__mapDeps([57,0,1,2,3,4,5,6]) : void 0);
      return withNativeThenUADE(
        "pumaTracker",
        ctx,
        (bytes, name) => parsePumaTrackerFile(bytes instanceof Uint8Array ? bytes.buffer : bytes, name),
        "PumaTrackerParser",
        { isFormat: (b) => isPumaTrackerFormat(b.buffer), injectUADE: true }
      );
    }
  }
  if (matchesExt(filename, ["syn"])) {
    const { isSynthesisFormat, parseSynthesisFile } = await __vitePreload(async () => {
      const { isSynthesisFormat: isSynthesisFormat2, parseSynthesisFile: parseSynthesisFile2 } = await import("./SynthesisParser-K-SNBo6N.js");
      return { isSynthesisFormat: isSynthesisFormat2, parseSynthesisFile: parseSynthesisFile2 };
    }, true ? __vite__mapDeps([58,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "synthesis",
      ctx,
      (bytes, name) => parseSynthesisFile(bytes, name),
      "SynthesisParser",
      { isFormat: isSynthesisFormat, usesBytes: true, injectUADE: true }
    );
  }
  if (matchesExt(filename, ["dss"])) {
    const { isDigitalSoundStudioFormat, parseDigitalSoundStudioFile } = await __vitePreload(async () => {
      const { isDigitalSoundStudioFormat: isDigitalSoundStudioFormat2, parseDigitalSoundStudioFile: parseDigitalSoundStudioFile2 } = await import("./DigitalSoundStudioParser-CT3T15d5.js");
      return { isDigitalSoundStudioFormat: isDigitalSoundStudioFormat2, parseDigitalSoundStudioFile: parseDigitalSoundStudioFile2 };
    }, true ? __vite__mapDeps([59,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "digitalSoundStudio",
      ctx,
      (bytes, name) => parseDigitalSoundStudioFile(bytes, name),
      "DigitalSoundStudioParser",
      { isFormat: isDigitalSoundStudioFormat, usesBytes: true, injectUADE: true }
    );
  }
  if (matchesExt(filename, ["ma"])) {
    const { isMusicAssemblerFormat, parseMusicAssemblerFile } = await __vitePreload(async () => {
      const { isMusicAssemblerFormat: isMusicAssemblerFormat2, parseMusicAssemblerFile: parseMusicAssemblerFile2 } = await import("./MusicAssemblerParser-COvMS3Ra.js");
      return { isMusicAssemblerFormat: isMusicAssemblerFormat2, parseMusicAssemblerFile: parseMusicAssemblerFile2 };
    }, true ? __vite__mapDeps([60,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "musicAssembler",
      ctx,
      (bytes, name) => parseMusicAssemblerFile(bytes, name),
      "MusicAssemblerParser",
      { isFormat: isMusicAssemblerFormat, usesBytes: true, injectUADE: true }
    );
  }
  if (matchesExt(filename, ["667"])) {
    if (prefs.composer667 === "native") {
      try {
        const { isComposer667Format, parseComposer667File } = await __vitePreload(async () => {
          const { isComposer667Format: isComposer667Format2, parseComposer667File: parseComposer667File2 } = await import("./Composer667Parser-D4s4kZ0b.js");
          return { isComposer667Format: isComposer667Format2, parseComposer667File: parseComposer667File2 };
        }, true ? __vite__mapDeps([61,0,1,2,3,4,5,6,62]) : void 0);
        const bytes = new Uint8Array(buffer);
        if (isComposer667Format(bytes)) {
          const result = parseComposer667File(bytes, originalFileName);
          if (result) {
            result.libopenmptFileData = buffer.slice(0);
            return result;
          }
        }
      } catch (err) {
        console.warn(`[Composer667Parser] Native parse failed for ${filename}, falling back to libopenmpt:`, err);
      }
    }
  }
  if (matchesExt(filename, ["cba"])) {
    const { parseWithOpenMPT } = await __vitePreload(async () => {
      const { parseWithOpenMPT: parseWithOpenMPT2 } = await import("./OpenMPTConverter--7ALLBUZ.js");
      return { parseWithOpenMPT: parseWithOpenMPT2 };
    }, true ? __vite__mapDeps([21,0,1,2,3,4,5,6,22]) : void 0);
    const song = await parseWithOpenMPT(buffer, originalFileName);
    song.libopenmptFileData = buffer.slice(0);
    return song;
  }
  if (matchesExt(filename, ["bd"])) {
    const { isBenDaglishFormat, parseBenDaglishFile } = await __vitePreload(async () => {
      const { isBenDaglishFormat: isBenDaglishFormat2, parseBenDaglishFile: parseBenDaglishFile2 } = await import("./BenDaglishParser-B8qfgNJC.js");
      return { isBenDaglishFormat: isBenDaglishFormat2, parseBenDaglishFile: parseBenDaglishFile2 };
    }, true ? __vite__mapDeps([63,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "benDaglish",
      ctx,
      (buf, name) => {
        const ab = buf instanceof Uint8Array ? buf.buffer : buf;
        if (isBenDaglishFormat(ab, name)) return parseBenDaglishFile(ab, name);
        return null;
      },
      "BenDaglishParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["ims"])) {
    const { parseWithOpenMPT } = await __vitePreload(async () => {
      const { parseWithOpenMPT: parseWithOpenMPT2 } = await import("./OpenMPTConverter--7ALLBUZ.js");
      return { parseWithOpenMPT: parseWithOpenMPT2 };
    }, true ? __vite__mapDeps([21,0,1,2,3,4,5,6,22]) : void 0);
    const song = await parseWithOpenMPT(buffer, originalFileName);
    song.libopenmptFileData = buffer.slice(0);
    song.uadeEditableFileData = buffer.slice(0);
    song.uadeEditableFileName = originalFileName;
    return song;
  }
  if (matchesExt(filename, ["ice"])) {
    const { parseWithOpenMPT } = await __vitePreload(async () => {
      const { parseWithOpenMPT: parseWithOpenMPT2 } = await import("./OpenMPTConverter--7ALLBUZ.js");
      return { parseWithOpenMPT: parseWithOpenMPT2 };
    }, true ? __vite__mapDeps([21,0,1,2,3,4,5,6,22]) : void 0);
    const song = await parseWithOpenMPT(buffer, originalFileName);
    song.libopenmptFileData = buffer.slice(0);
    return song;
  }
  if (matchesExt(filename, ["kris"])) {
    const { parseWithOpenMPT } = await __vitePreload(async () => {
      const { parseWithOpenMPT: parseWithOpenMPT2 } = await import("./OpenMPTConverter--7ALLBUZ.js");
      return { parseWithOpenMPT: parseWithOpenMPT2 };
    }, true ? __vite__mapDeps([21,0,1,2,3,4,5,6,22]) : void 0);
    const song = await parseWithOpenMPT(buffer, originalFileName);
    song.libopenmptFileData = buffer.slice(0);
    return song;
  }
  if (matchesExt(filename, ["ml"])) {
    const bytes = new Uint8Array(buffer);
    try {
      const { isMusicLineFile, parseMusicLineFile } = await __vitePreload(async () => {
        const { isMusicLineFile: isMusicLineFile2, parseMusicLineFile: parseMusicLineFile2 } = await import("./main-BbV5VyEH.js").then((n) => n.jr);
        return { isMusicLineFile: isMusicLineFile2, parseMusicLineFile: parseMusicLineFile2 };
      }, true ? __vite__mapDeps([0,1,2,3,4,5,6]) : void 0);
      if (isMusicLineFile(bytes)) {
        const result = parseMusicLineFile(bytes);
        if (result) return result;
      }
    } catch (err) {
      console.warn(`[MusicLineParser] Native parse failed for ${filename}:`, err);
    }
  }
  if (matchesExt(filename, ["gmc"])) {
    const { parseWithOpenMPT } = await __vitePreload(async () => {
      const { parseWithOpenMPT: parseWithOpenMPT2 } = await import("./OpenMPTConverter--7ALLBUZ.js");
      return { parseWithOpenMPT: parseWithOpenMPT2 };
    }, true ? __vite__mapDeps([21,0,1,2,3,4,5,6,22]) : void 0);
    const song = await parseWithOpenMPT(buffer, originalFileName);
    song.libopenmptFileData = buffer.slice(0);
    return song;
  }
  if (matchesExt(filename, ["ftm"])) {
    const { parseWithOpenMPT } = await __vitePreload(async () => {
      const { parseWithOpenMPT: parseWithOpenMPT2 } = await import("./OpenMPTConverter--7ALLBUZ.js");
      return { parseWithOpenMPT: parseWithOpenMPT2 };
    }, true ? __vite__mapDeps([21,0,1,2,3,4,5,6,22]) : void 0);
    const song = await parseWithOpenMPT(buffer, originalFileName);
    song.libopenmptFileData = buffer.slice(0);
    return song;
  }
  if (matchesExt(filename, ["st"])) {
    const { isSawteethFormat, parseSawteethFile } = await __vitePreload(async () => {
      const { isSawteethFormat: isSawteethFormat2, parseSawteethFile: parseSawteethFile2 } = await import("./SawteethParser-Bf_OwRXQ.js");
      return { isSawteethFormat: isSawteethFormat2, parseSawteethFile: parseSawteethFile2 };
    }, true ? __vite__mapDeps([64,0,1,2,3,4,5,6]) : void 0);
    if (isSawteethFormat(new Uint8Array(buffer))) {
      return withNativeThenUADE(
        "sawteeth",
        ctx,
        (buf, name) => {
          if (isSawteethFormat(new Uint8Array(buf))) return parseSawteethFile(new Uint8Array(buf), name) ?? null;
          return null;
        },
        "SawteethParser",
        { injectUADE: true }
      );
    }
    const { parseUADEFile: parseUADE_st } = await __vitePreload(async () => {
      const { parseUADEFile: parseUADE_st2 } = await import("./main-BbV5VyEH.js").then((n) => n.ji);
      return { parseUADEFile: parseUADE_st2 };
    }, true ? __vite__mapDeps([0,1,2,3,4,5,6]) : void 0);
    return parseUADE_st(buffer, originalFileName, prefs.uade ?? "enhanced", subsong, preScannedMeta);
  }
  if (matchesExt(filename, ["sc", "sct"])) {
    const { isSoundControlFormat, parseSoundControlFile } = await __vitePreload(async () => {
      const { isSoundControlFormat: isSoundControlFormat2, parseSoundControlFile: parseSoundControlFile2 } = await import("./SoundControlParser-GBUd89o-.js");
      return { isSoundControlFormat: isSoundControlFormat2, parseSoundControlFile: parseSoundControlFile2 };
    }, true ? __vite__mapDeps([65,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "soundControl",
      ctx,
      (bytes, name) => parseSoundControlFile(bytes, name),
      "SoundControlParser",
      { isFormat: isSoundControlFormat, usesBytes: true, injectUADE: true }
    );
  }
  if (matchesExt(filename, ["psf"])) {
    const { isSoundFactoryFormat, parseSoundFactoryFile } = await __vitePreload(async () => {
      const { isSoundFactoryFormat: isSoundFactoryFormat2, parseSoundFactoryFile: parseSoundFactoryFile2 } = await import("./SoundFactoryParser-CbCkFfJ2.js");
      return { isSoundFactoryFormat: isSoundFactoryFormat2, parseSoundFactoryFile: parseSoundFactoryFile2 };
    }, true ? __vite__mapDeps([66,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "soundFactory",
      ctx,
      (buf, name) => {
        const u8 = buf instanceof Uint8Array ? buf : new Uint8Array(buf);
        if (isSoundFactoryFormat(u8)) return parseSoundFactoryFile(u8, name);
        return null;
      },
      "SoundFactoryParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["act", "ast"])) {
    const { isActionamicsFormat, parseActionamicsFile } = await __vitePreload(async () => {
      const { isActionamicsFormat: isActionamicsFormat2, parseActionamicsFile: parseActionamicsFile2 } = await import("./ActionamicsParser-ClbLJkoh.js");
      return { isActionamicsFormat: isActionamicsFormat2, parseActionamicsFile: parseActionamicsFile2 };
    }, true ? __vite__mapDeps([67,0,1,2,3,4,5,6,68]) : void 0);
    return withNativeThenUADE(
      "actionamics",
      ctx,
      (bytes, name) => parseActionamicsFile(bytes, name),
      "ActionamicsParser",
      { isFormat: isActionamicsFormat, usesBytes: true, injectUADE: true }
    );
  }
  if (matchesExt(filename, ["avp", "mw"])) {
    const { isActivisionProFormat, parseActivisionProFile } = await __vitePreload(async () => {
      const { isActivisionProFormat: isActivisionProFormat2, parseActivisionProFile: parseActivisionProFile2 } = await import("./ActivisionProParser-Dzt6J4vS.js");
      return { isActivisionProFormat: isActivisionProFormat2, parseActivisionProFile: parseActivisionProFile2 };
    }, true ? __vite__mapDeps([69,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "activisionPro",
      ctx,
      (bytes, name) => parseActivisionProFile(bytes, name),
      "ActivisionProParser",
      { isFormat: isActivisionProFormat, usesBytes: true, injectUADE: true }
    );
  }
  if (matchesExt(filename, ["rk", "rkb"])) {
    const { isRonKlarenFormat, parseRonKlarenFile } = await __vitePreload(async () => {
      const { isRonKlarenFormat: isRonKlarenFormat2, parseRonKlarenFile: parseRonKlarenFile2 } = await import("./RonKlarenParser-Dv0g6N1b.js");
      return { isRonKlarenFormat: isRonKlarenFormat2, parseRonKlarenFile: parseRonKlarenFile2 };
    }, true ? __vite__mapDeps([70,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "ronKlaren",
      ctx,
      (buf, name) => {
        const u8 = buf instanceof Uint8Array ? buf : new Uint8Array(buf);
        if (isRonKlarenFormat(u8)) return parseRonKlarenFile(u8, name);
        return null;
      },
      "RonKlarenParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["unic"])) {
    try {
      const { isUNICFormat, parseUNICFile } = await __vitePreload(async () => {
        const { isUNICFormat: isUNICFormat2, parseUNICFile: parseUNICFile2 } = await import("./UNICParser-BSHcmGDb.js");
        return { isUNICFormat: isUNICFormat2, parseUNICFile: parseUNICFile2 };
      }, true ? __vite__mapDeps([71,0,1,2,3,4,5,6]) : void 0);
      if (isUNICFormat(buffer)) {
        const song = await parseUNICFile(buffer, originalFileName);
        song.libopenmptFileData = buffer.slice(0);
        return song;
      }
    } catch (err) {
      console.warn(`[UNICParser] Native parse failed for ${filename}, falling back to OpenMPT:`, err);
    }
  }
  if (matchesExt(filename, ["mtm"])) {
    try {
      const { isMTMFormat, parseMTMFile } = await __vitePreload(async () => {
        const { isMTMFormat: isMTMFormat2, parseMTMFile: parseMTMFile2 } = await import("./MTMParser-B0eVSB7X.js");
        return { isMTMFormat: isMTMFormat2, parseMTMFile: parseMTMFile2 };
      }, true ? __vite__mapDeps([72,0,1,2,3,4,5,6]) : void 0);
      if (isMTMFormat(buffer)) {
        const song = await parseMTMFile(buffer, originalFileName);
        song.libopenmptFileData = buffer.slice(0);
        return song;
      }
    } catch (err) {
      console.warn(`[MTMParser] Native parse failed for ${filename}, falling back to OpenMPT:`, err);
    }
  }
  if (matchesExt(filename, ["669"])) {
    try {
      const { is669Format, parse669File } = await __vitePreload(async () => {
        const { is669Format: is669Format2, parse669File: parse669File2 } = await import("./Format669Parser-D4FObzyF.js");
        return { is669Format: is669Format2, parse669File: parse669File2 };
      }, true ? __vite__mapDeps([73,0,1,2,3,4,5,6,62]) : void 0);
      if (is669Format(buffer)) {
        const song = await parse669File(buffer, originalFileName);
        song.libopenmptFileData = buffer.slice(0);
        return song;
      }
    } catch (err) {
      console.warn(`[Format669Parser] Native parse failed for ${filename}, falling back to OpenMPT:`, err);
    }
  }
  if (matchesExt(filename, ["far"])) {
    try {
      const { isFARFormat, parseFARFile } = await __vitePreload(async () => {
        const { isFARFormat: isFARFormat2, parseFARFile: parseFARFile2 } = await import("./FARParser-CcW-dv38.js");
        return { isFARFormat: isFARFormat2, parseFARFile: parseFARFile2 };
      }, true ? __vite__mapDeps([74,0,1,2,3,4,5,6]) : void 0);
      if (isFARFormat(buffer)) {
        const song = await parseFARFile(buffer, originalFileName);
        song.libopenmptFileData = buffer.slice(0);
        return song;
      }
    } catch (err) {
      console.warn(`[FARParser] Native parse failed for ${filename}, falling back to OpenMPT:`, err);
    }
  }
  if (matchesExt(filename, ["plm"])) {
    try {
      const { isPLMFormat, parsePLMFile } = await __vitePreload(async () => {
        const { isPLMFormat: isPLMFormat2, parsePLMFile: parsePLMFile2 } = await import("./PLMParser-B18M4plG.js");
        return { isPLMFormat: isPLMFormat2, parsePLMFile: parsePLMFile2 };
      }, true ? __vite__mapDeps([75,0,1,2,3,4,5,6]) : void 0);
      if (isPLMFormat(buffer)) {
        const song = await parsePLMFile(buffer, originalFileName);
        song.libopenmptFileData = buffer.slice(0);
        return song;
      }
    } catch (err) {
      console.warn(`[PLMParser] Native parse failed for ${filename}, falling back to OpenMPT:`, err);
    }
  }
  if (matchesExt(filename, ["ult"])) {
    try {
      const { isULTFormat, parseULTFile } = await __vitePreload(async () => {
        const { isULTFormat: isULTFormat2, parseULTFile: parseULTFile2 } = await import("./ULTParser-QuM6OqAJ.js");
        return { isULTFormat: isULTFormat2, parseULTFile: parseULTFile2 };
      }, true ? __vite__mapDeps([76,0,1,2,3,4,5,6]) : void 0);
      if (isULTFormat(buffer)) {
        const song = await parseULTFile(buffer, originalFileName);
        song.libopenmptFileData = buffer.slice(0);
        return song;
      }
    } catch (err) {
      console.warn(`[ULTParser] Native parse failed for ${filename}, falling back to OpenMPT:`, err);
    }
  }
  if (matchesExt(filename, ["rtm"])) {
    try {
      const { isRTMFormat, parseRTMFile } = await __vitePreload(async () => {
        const { isRTMFormat: isRTMFormat2, parseRTMFile: parseRTMFile2 } = await import("./RTMParser-CNEnSXvi.js");
        return { isRTMFormat: isRTMFormat2, parseRTMFile: parseRTMFile2 };
      }, true ? __vite__mapDeps([77,0,1,2,3,4,5,6]) : void 0);
      if (isRTMFormat(buffer)) {
        const song = await parseRTMFile(buffer, originalFileName);
        song.libopenmptFileData = buffer.slice(0);
        return song;
      }
    } catch (err) {
      console.warn(`[RTMParser] Native parse failed for ${filename}, falling back to OpenMPT:`, err);
    }
  }
  if (matchesExt(filename, ["dsm"])) {
    try {
      const { isDSMFormat, parseDSMFile } = await __vitePreload(async () => {
        const { isDSMFormat: isDSMFormat2, parseDSMFile: parseDSMFile2 } = await import("./DSMParser-WhYn6SNV.js");
        return { isDSMFormat: isDSMFormat2, parseDSMFile: parseDSMFile2 };
      }, true ? __vite__mapDeps([78,0,1,2,3,4,5,6]) : void 0);
      if (isDSMFormat(buffer)) {
        const song = await parseDSMFile(buffer, originalFileName);
        song.libopenmptFileData = buffer.slice(0);
        return song;
      }
    } catch (err) {
      console.warn(`[DSMParser] Native parse failed for ${filename}, falling back to OpenMPT:`, err);
    }
  }
  if (matchesExt(filename, ["dtm"])) {
    try {
      const { isDTMFormat, parseDTMFile } = await __vitePreload(async () => {
        const { isDTMFormat: isDTMFormat2, parseDTMFile: parseDTMFile2 } = await import("./DTMParser-CuQm3u8Z.js");
        return { isDTMFormat: isDTMFormat2, parseDTMFile: parseDTMFile2 };
      }, true ? __vite__mapDeps([79,0,1,2,3,4,5,6]) : void 0);
      if (isDTMFormat(buffer)) {
        const song = await parseDTMFile(buffer, originalFileName);
        song.libopenmptFileData = buffer.slice(0);
        return song;
      }
    } catch (err) {
      console.warn(`[DTMParser] Native parse failed for ${filename}, falling back to OpenMPT:`, err);
    }
  }
  if (matchesExt(filename, ["stm"])) {
    try {
      const { isSTMFormat, parseSTMFile } = await __vitePreload(async () => {
        const { isSTMFormat: isSTMFormat2, parseSTMFile: parseSTMFile2 } = await import("./STMParser-7BTVgRh_.js");
        return { isSTMFormat: isSTMFormat2, parseSTMFile: parseSTMFile2 };
      }, true ? __vite__mapDeps([80,0,1,2,3,4,5,6]) : void 0);
      if (isSTMFormat(buffer)) {
        const song = await parseSTMFile(buffer, originalFileName);
        song.libopenmptFileData = buffer.slice(0);
        return song;
      }
    } catch (err) {
      console.warn(`[STMParser] Native parse failed for ${filename}, falling back to OpenMPT:`, err);
    }
  }
  if (matchesExt(filename, ["stx"])) {
    try {
      const { isSTXFormat, parseSTXFile } = await __vitePreload(async () => {
        const { isSTXFormat: isSTXFormat2, parseSTXFile: parseSTXFile2 } = await import("./STXParser-DT6Ybgnl.js");
        return { isSTXFormat: isSTXFormat2, parseSTXFile: parseSTXFile2 };
      }, true ? __vite__mapDeps([81,0,1,2,3,4,5,6]) : void 0);
      if (isSTXFormat(buffer)) {
        const song = parseSTXFile(buffer, originalFileName);
        song.libopenmptFileData = buffer.slice(0);
        return song;
      }
    } catch (err) {
      console.warn(`[STXParser] Native parse failed for ${filename}, falling back to OpenMPT:`, err);
    }
  }
  if (matchesExt(filename, ["nru"])) {
    try {
      const { isNRUFormat, parseNRUFile } = await __vitePreload(async () => {
        const { isNRUFormat: isNRUFormat2, parseNRUFile: parseNRUFile2 } = await import("./NRUParser-BWRqipWt.js");
        return { isNRUFormat: isNRUFormat2, parseNRUFile: parseNRUFile2 };
      }, true ? __vite__mapDeps([82,0,1,2,3,4,5,6]) : void 0);
      if (isNRUFormat(buffer)) {
        const song = await parseNRUFile(buffer, originalFileName);
        song.libopenmptFileData = buffer.slice(0);
        return song;
      }
    } catch (err) {
      console.warn(`[NRUParser] Native parse failed for ${filename}, falling back to OpenMPT:`, err);
    }
  }
  if (matchesExt(filename, ["ptm"])) {
    try {
      const { isPTMFormat, parsePTMFile } = await __vitePreload(async () => {
        const { isPTMFormat: isPTMFormat2, parsePTMFile: parsePTMFile2 } = await import("./PTMParser-gKUwOnXN.js");
        return { isPTMFormat: isPTMFormat2, parsePTMFile: parsePTMFile2 };
      }, true ? __vite__mapDeps([83,0,1,2,3,4,5,6]) : void 0);
      if (isPTMFormat(buffer)) {
        const song = await parsePTMFile(buffer, originalFileName);
        song.libopenmptFileData = buffer.slice(0);
        return song;
      }
    } catch (err) {
      console.warn(`[PTMParser] Native parse failed for ${filename}, falling back to OpenMPT:`, err);
    }
  }
  if (matchesExt(filename, ["gdm"])) {
    try {
      const { isGDMFormat, parseGDMFile } = await __vitePreload(async () => {
        const { isGDMFormat: isGDMFormat2, parseGDMFile: parseGDMFile2 } = await import("./GDMParser-BUfX8k8-.js");
        return { isGDMFormat: isGDMFormat2, parseGDMFile: parseGDMFile2 };
      }, true ? __vite__mapDeps([84,0,1,2,3,4,5,6]) : void 0);
      if (isGDMFormat(buffer)) {
        const song = await parseGDMFile(buffer, originalFileName);
        song.libopenmptFileData = buffer.slice(0);
        return song;
      }
    } catch (err) {
      console.warn(`[GDMParser] Native parse failed for ${filename}, falling back to OpenMPT:`, err);
    }
  }
  if (matchesExt(filename, ["stk"])) {
    try {
      const { isSTKFormat, parseSTKFile } = await __vitePreload(async () => {
        const { isSTKFormat: isSTKFormat2, parseSTKFile: parseSTKFile2 } = await import("./STKParser-BGUKbLEP.js");
        return { isSTKFormat: isSTKFormat2, parseSTKFile: parseSTKFile2 };
      }, true ? __vite__mapDeps([85,0,1,2,3,4,5,6]) : void 0);
      if (isSTKFormat(buffer)) {
        const song = await parseSTKFile(buffer, originalFileName);
        song.libopenmptFileData = buffer.slice(0);
        return song;
      }
    } catch (err) {
      console.warn(`[STKParser] Native parse failed for ${filename}, falling back to OpenMPT:`, err);
    }
  }
  if (matchesExt(filename, ["stp"])) {
    try {
      const { isSTPFormat, parseSTPFile } = await __vitePreload(async () => {
        const { isSTPFormat: isSTPFormat2, parseSTPFile: parseSTPFile2 } = await import("./STPParser-BC6euN58.js");
        return { isSTPFormat: isSTPFormat2, parseSTPFile: parseSTPFile2 };
      }, true ? __vite__mapDeps([86,0,1,2,3,4,5,6]) : void 0);
      if (isSTPFormat(buffer)) {
        const song = await parseSTPFile(buffer, originalFileName);
        song.libopenmptFileData = buffer.slice(0);
        return song;
      }
    } catch (err) {
      console.warn(`[STPParser] Native parse failed for ${filename}, falling back to OpenMPT:`, err);
    }
  }
  if (matchesExt(filename, ["mdl"])) {
    try {
      const { isMDLFormat, parseMDLFile } = await __vitePreload(async () => {
        const { isMDLFormat: isMDLFormat2, parseMDLFile: parseMDLFile2 } = await import("./MDLParser-Di9ZWSE7.js");
        return { isMDLFormat: isMDLFormat2, parseMDLFile: parseMDLFile2 };
      }, true ? __vite__mapDeps([87,0,1,2,3,4,5,6]) : void 0);
      if (isMDLFormat(buffer)) {
        const song = await parseMDLFile(buffer, originalFileName);
        song.libopenmptFileData = buffer.slice(0);
        return song;
      }
    } catch (err) {
      console.warn(`[MDLParser] Native parse failed for ${filename}, falling back to OpenMPT:`, err);
    }
  }
  if (matchesExt(filename, ["amf"])) {
    try {
      const { isAMFFormat, parseAMFFile } = await __vitePreload(async () => {
        const { isAMFFormat: isAMFFormat2, parseAMFFile: parseAMFFile2 } = await import("./AMFParser-C-PGXJmB.js");
        return { isAMFFormat: isAMFFormat2, parseAMFFile: parseAMFFile2 };
      }, true ? __vite__mapDeps([88,0,1,2,3,4,5,6]) : void 0);
      if (isAMFFormat(buffer)) {
        const song = await parseAMFFile(buffer, originalFileName);
        song.libopenmptFileData = buffer.slice(0);
        return song;
      }
    } catch (err) {
      console.warn(`[AMFParser] Native parse failed for ${filename}, falling back to OpenMPT:`, err);
    }
  }
  if (matchesExt(filename, ["imf"])) {
    if (prefs.imagoOrpheus === "native") {
      try {
        const { isImagoOrpheusFormat, parseImagoOrpheusFile } = await __vitePreload(async () => {
          const { isImagoOrpheusFormat: isImagoOrpheusFormat2, parseImagoOrpheusFile: parseImagoOrpheusFile2 } = await import("./ImagoOrpheusParser-DMCtLcLW.js");
          return { isImagoOrpheusFormat: isImagoOrpheusFormat2, parseImagoOrpheusFile: parseImagoOrpheusFile2 };
        }, true ? __vite__mapDeps([89,0,1,2,3,4,5,6]) : void 0);
        const bytes = new Uint8Array(buffer);
        if (isImagoOrpheusFormat(bytes)) {
          const result = parseImagoOrpheusFile(bytes, originalFileName);
          if (result) {
            result.libopenmptFileData = buffer.slice(0);
            return result;
          }
        }
      } catch (err) {
        console.warn(`[ImagoOrpheusParser] Native parse failed for ${filename}, falling back to OpenMPT:`, err);
      }
    }
  }
  if (matchesExt(filename, ["c67"])) {
    if (prefs.cdfm67 === "native") {
      try {
        const { isCDFM67Format, parseCDFM67File } = await __vitePreload(async () => {
          const { isCDFM67Format: isCDFM67Format2, parseCDFM67File: parseCDFM67File2 } = await import("./CDFM67Parser-DguyM7Ic.js");
          return { isCDFM67Format: isCDFM67Format2, parseCDFM67File: parseCDFM67File2 };
        }, true ? __vite__mapDeps([90,0,1,2,3,4,5,6,91]) : void 0);
        const bytes = new Uint8Array(buffer);
        if (isCDFM67Format(bytes)) {
          const result = parseCDFM67File(bytes, originalFileName);
          if (result) {
            result.libopenmptFileData = buffer.slice(0);
            return result;
          }
        }
      } catch (err) {
        console.warn(`[CDFM67Parser] Native parse failed for ${filename}, falling back to OpenMPT:`, err);
      }
    }
  }
  if (matchesExt(filename, ["etx"])) {
    if (prefs.easyTrax === "native") {
      try {
        const { isEasyTraxFormat, parseEasyTraxFile } = await __vitePreload(async () => {
          const { isEasyTraxFormat: isEasyTraxFormat2, parseEasyTraxFile: parseEasyTraxFile2 } = await import("./EasyTraxParser-CDEYq2Z2.js");
          return { isEasyTraxFormat: isEasyTraxFormat2, parseEasyTraxFile: parseEasyTraxFile2 };
        }, true ? [] : void 0);
        const bytes = new Uint8Array(buffer);
        if (isEasyTraxFormat(bytes)) {
          const result = parseEasyTraxFile(bytes, originalFileName);
          if (result) {
            result.libopenmptFileData = buffer.slice(0);
            return result;
          }
        }
      } catch (err) {
        console.warn(`[EasyTraxParser] Native parse failed for ${filename}, falling back to OpenMPT:`, err);
      }
    }
  }
  if (matchesExt(filename, ["mus"]) && prefs.karlMorton === "native") {
    try {
      const { isKarlMortonFormat, parseKarlMortonFile } = await __vitePreload(async () => {
        const { isKarlMortonFormat: isKarlMortonFormat2, parseKarlMortonFile: parseKarlMortonFile2 } = await import("./KarlMortonParser-NBEvWyW9.js");
        return { isKarlMortonFormat: isKarlMortonFormat2, parseKarlMortonFile: parseKarlMortonFile2 };
      }, true ? __vite__mapDeps([92,0,1,2,3,4,5,6]) : void 0);
      const bytes = new Uint8Array(buffer);
      if (isKarlMortonFormat(bytes)) {
        const result = parseKarlMortonFile(bytes, originalFileName);
        if (result) {
          result.libopenmptFileData = buffer.slice(0);
          return result;
        }
      }
    } catch (err) {
      console.warn(`[KarlMortonParser] Native parse failed for ${filename}:`, err);
    }
  }
  if (matchesExt(filename, ["ufo", "mus"])) {
    const { isUFOFormat, parseUFOFile } = await __vitePreload(async () => {
      const { isUFOFormat: isUFOFormat2, parseUFOFile: parseUFOFile2 } = await import("./UFOParser-ChvUeJ1Z.js");
      return { isUFOFormat: isUFOFormat2, parseUFOFile: parseUFOFile2 };
    }, true ? [] : void 0);
    return withNativeThenUADE(
      "ufo",
      ctx,
      (buf, name) => {
        const ab = buf instanceof Uint8Array ? buf.buffer : buf;
        if (isUFOFormat(buf)) return parseUFOFile(ab, name);
        return null;
      },
      "UFOParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["xmf"])) {
    if (prefs.xmf === "native") {
      try {
        const { isXMFFormat, parseXMFFile } = await __vitePreload(async () => {
          const { isXMFFormat: isXMFFormat2, parseXMFFile: parseXMFFile2 } = await import("./XMFParser-B98Hvq95.js");
          return { isXMFFormat: isXMFFormat2, parseXMFFile: parseXMFFile2 };
        }, true ? __vite__mapDeps([93,0,1,2,3,4,5,6,94]) : void 0);
        const bytes = new Uint8Array(buffer);
        if (isXMFFormat(bytes)) {
          const result = parseXMFFile(bytes, originalFileName);
          if (result) {
            result.libopenmptFileData = buffer.slice(0);
            return result;
          }
        }
      } catch (err) {
        console.warn(`[XMFParser] Native parse failed for ${filename}, falling back to OpenMPT:`, err);
      }
    }
  }
  if (matchesExt(filename, ["uax"])) {
    if (prefs.uax === "native") {
      try {
        const { isUAXFormat, parseUAXFile } = await __vitePreload(async () => {
          const { isUAXFormat: isUAXFormat2, parseUAXFile: parseUAXFile2 } = await import("./UAXParser-Bv-tuGCE.js");
          return { isUAXFormat: isUAXFormat2, parseUAXFile: parseUAXFile2 };
        }, true ? [] : void 0);
        const bytes = new Uint8Array(buffer);
        if (isUAXFormat(bytes)) {
          const result = parseUAXFile(bytes, originalFileName);
          if (result) {
            result.libopenmptFileData = buffer.slice(0);
            return result;
          }
        }
      } catch (err) {
        throw new Error(`[UAXParser] Failed to parse ${filename}: ${err}`);
      }
    }
    throw new Error(`[UAXParser] ${filename}: no native parser available or format not recognised`);
  }
  if (matchesExt(filename, ["fmt"])) {
    try {
      const { isFMTrackerFormat, parseFMTrackerFile } = await __vitePreload(async () => {
        const { isFMTrackerFormat: isFMTrackerFormat2, parseFMTrackerFile: parseFMTrackerFile2 } = await import("./FMTrackerParser-DHLFZ_tw.js");
        return { isFMTrackerFormat: isFMTrackerFormat2, parseFMTrackerFile: parseFMTrackerFile2 };
      }, true ? [] : void 0);
      const bytes = new Uint8Array(buffer);
      if (isFMTrackerFormat(bytes)) {
        const result = parseFMTrackerFile(bytes, originalFileName);
        if (result) {
          result.libopenmptFileData = buffer.slice(0);
          return result;
        }
      }
    } catch (err) {
      console.warn(`[FMTrackerParser] Native parse failed for ${filename}, falling back to libopenmpt:`, err);
    }
  }
  if (matchesExt(filename, ["mt2"])) {
    if (prefs.madTracker2 === "native") {
      try {
        const { isMadTracker2Format, parseMadTracker2File } = await __vitePreload(async () => {
          const { isMadTracker2Format: isMadTracker2Format2, parseMadTracker2File: parseMadTracker2File2 } = await import("./MadTracker2Parser-pgsRrRhj.js");
          return { isMadTracker2Format: isMadTracker2Format2, parseMadTracker2File: parseMadTracker2File2 };
        }, true ? __vite__mapDeps([95,0,1,2,3,4,5,6]) : void 0);
        const bytes = new Uint8Array(buffer);
        if (isMadTracker2Format(bytes)) {
          const result = parseMadTracker2File(bytes, originalFileName);
          if (result) {
            result.libopenmptFileData = buffer.slice(0);
            return result;
          }
        }
      } catch (err) {
        console.warn(`[MadTracker2Parser] Native parse failed for ${filename}, falling back to libopenmpt:`, err);
      }
    }
  }
  if (matchesExt(filename, ["psm"]) && prefs.psm !== "uade") {
    try {
      const { isPSMFormat, parsePSMFile } = await __vitePreload(async () => {
        const { isPSMFormat: isPSMFormat2, parsePSMFile: parsePSMFile2 } = await import("./PSMParser-CoFrjjEl.js");
        return { isPSMFormat: isPSMFormat2, parsePSMFile: parsePSMFile2 };
      }, true ? __vite__mapDeps([96,0,1,2,3,4,5,6]) : void 0);
      const bytes = new Uint8Array(buffer);
      if (isPSMFormat(bytes)) {
        const result = parsePSMFile(bytes, originalFileName);
        if (result) {
          result.libopenmptFileData = buffer.slice(0);
          return result;
        }
      }
    } catch (err) {
      console.warn(`[PSMParser] Native parse failed for ${filename}, falling back:`, err);
    }
  }
  if (matchesExt(filename, ["ams"])) {
    const { parseWithOpenMPT } = await __vitePreload(async () => {
      const { parseWithOpenMPT: parseWithOpenMPT2 } = await import("./OpenMPTConverter--7ALLBUZ.js");
      return { parseWithOpenMPT: parseWithOpenMPT2 };
    }, true ? __vite__mapDeps([21,0,1,2,3,4,5,6,22]) : void 0);
    const song = await parseWithOpenMPT(buffer, originalFileName);
    song.libopenmptFileData = buffer.slice(0);
    return song;
  }
  if (matchesExt(filename, ["smus", "snx", "tiny"])) {
    if (prefs.iffSmus === "native") {
      try {
        const { isIffSmusFormat, parseIffSmusFile } = await __vitePreload(async () => {
          const { isIffSmusFormat: isIffSmusFormat2, parseIffSmusFile: parseIffSmusFile2 } = await import("./IffSmusParser-CFXS4lKv.js");
          return { isIffSmusFormat: isIffSmusFormat2, parseIffSmusFile: parseIffSmusFile2 };
        }, true ? __vite__mapDeps([97,0,1,2,3,4,5,6]) : void 0);
        if (isIffSmusFormat(buffer)) {
          const smusResult = await parseIffSmusFile(buffer, originalFileName, companionFiles);
          const hasAudio = smusResult.instruments.some(
            (inst) => {
              var _a2;
              return ((_a2 = inst.sample) == null ? void 0 : _a2.audioBuffer) && inst.sample.audioBuffer.byteLength > 100;
            }
          );
          if (hasAudio) return smusResult;
          console.warn(`[IffSmusParser] All instruments silent (no .ss companions), falling back to UADE`);
        }
      } catch (err) {
        console.warn(`[IffSmusParser] Native parse failed for ${filename}, falling back to UADE:`, err);
      }
      try {
        const { isSonixFormat, parseSonixFile } = await __vitePreload(async () => {
          const { isSonixFormat: isSonixFormat2, parseSonixFile: parseSonixFile2 } = await import("./SonixMusicDriverParser-CmlnKfh2.js");
          return { isSonixFormat: isSonixFormat2, parseSonixFile: parseSonixFile2 };
        }, true ? __vite__mapDeps([98,0,1,2,3,4,5,6]) : void 0);
        if (isSonixFormat(buffer)) return await parseSonixFile(buffer, originalFileName);
      } catch (err) {
        console.warn(`[SonixMusicDriverParser] Native parse failed for ${filename}, falling back to UADE:`, err);
      }
    }
    const { parseUADEFile: parseUADE_smus } = await __vitePreload(async () => {
      const { parseUADEFile: parseUADE_smus2 } = await import("./main-BbV5VyEH.js").then((n) => n.ji);
      return { parseUADEFile: parseUADE_smus2 };
    }, true ? __vite__mapDeps([0,1,2,3,4,5,6]) : void 0);
    return parseUADE_smus(buffer, originalFileName, prefs.uade ?? "enhanced", subsong, preScannedMeta);
  }
  if (matchesExt(filename, ["mfp"])) {
    const [{ isMFPFormat, parseMFPFile }, { isMagneticFieldsPackerFormat, parseMagneticFieldsPackerFile }] = await Promise.all([
      __vitePreload(() => import("./MFPParser-DrcqVC3G.js"), true ? __vite__mapDeps([99,0,1,2,3,4,5,6]) : void 0),
      __vitePreload(() => import("./MagneticFieldsPackerParser-CzJCTLV5.js"), true ? [] : void 0)
    ]);
    return withNativeThenUADE(
      "magneticFieldsPacker",
      ctx,
      async (buf, name) => {
        if (isMFPFormat(buf, name)) return await parseMFPFile(buf, name);
        if (isMagneticFieldsPackerFormat(buf, name)) return await parseMagneticFieldsPackerFile(buf, name);
        return null;
      },
      "MFPParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["dm", "dm1"])) {
    const { isDeltaMusic1Format, parseDeltaMusic1File } = await __vitePreload(async () => {
      const { isDeltaMusic1Format: isDeltaMusic1Format2, parseDeltaMusic1File: parseDeltaMusic1File2 } = await import("./DeltaMusic1Parser-DoSsyp-W.js");
      return { isDeltaMusic1Format: isDeltaMusic1Format2, parseDeltaMusic1File: parseDeltaMusic1File2 };
    }, true ? __vite__mapDeps([100,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "deltaMusic1",
      ctx,
      async (buf, name) => {
        if (isDeltaMusic1Format(buf)) return await parseDeltaMusic1File(buf, name) ?? null;
        return null;
      },
      "DeltaMusic1Parser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["rjp"]) || matchesExt(filename, ["sng"]) && buffer.byteLength >= 16 && new Uint8Array(buffer)[0] === 82 && // 'R'
  new Uint8Array(buffer)[1] === 74 && // 'J'
  new Uint8Array(buffer)[2] === 80) {
    const { isRJPFormat, parseRJPFile } = await __vitePreload(async () => {
      const { isRJPFormat: isRJPFormat2, parseRJPFile: parseRJPFile2 } = await import("./RichardJosephParser-BsgWQiRW.js");
      return { isRJPFormat: isRJPFormat2, parseRJPFile: parseRJPFile2 };
    }, true ? __vite__mapDeps([101,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "richardJoseph",
      ctx,
      (buf, name) => {
        if (isRJPFormat(buf instanceof Uint8Array ? buf : new Uint8Array(buf))) {
          return parseRJPFile(buf instanceof Uint8Array ? buf.buffer : buf, name, companionFiles);
        }
        return null;
      },
      "RichardJosephParser",
      { injectUADE: true }
    );
  }
  if (prefs.pt36 === "native" && buffer.byteLength >= 12) {
    const _pt36 = new Uint8Array(buffer);
    if (_pt36[0] === 70 && _pt36[1] === 79 && _pt36[2] === 82 && _pt36[3] === 77 && _pt36[8] === 77 && _pt36[9] === 79 && _pt36[10] === 68 && _pt36[11] === 76) {
      try {
        const { parsePT36File } = await __vitePreload(async () => {
          const { parsePT36File: parsePT36File2 } = await import("./PT36Parser-DTMy0fTA.js");
          return { parsePT36File: parsePT36File2 };
        }, true ? __vite__mapDeps([102,0,1,2,3,4,5,6]) : void 0);
        return await parsePT36File(buffer, originalFileName);
      } catch (err) {
        console.warn(`[PT36Parser] Native parse failed for ${filename}, falling through to OpenMPT:`, err);
      }
    }
  }
  if (matchesExt(filename, ["ss"])) {
    const { isSpeedySystemFormat, parseSpeedySystemFile } = await __vitePreload(async () => {
      const { isSpeedySystemFormat: isSpeedySystemFormat2, parseSpeedySystemFile: parseSpeedySystemFile2 } = await import("./SpeedySystemParser-7E80EevE.js");
      return { isSpeedySystemFormat: isSpeedySystemFormat2, parseSpeedySystemFile: parseSpeedySystemFile2 };
    }, true ? [] : void 0);
    return withNativeThenUADE(
      "speedySystem",
      ctx,
      (buf, name) => {
        const ab = buf instanceof Uint8Array ? buf.buffer : buf;
        if (isSpeedySystemFormat(ab)) return parseSpeedySystemFile(ab, name);
        return null;
      },
      "SpeedySystemParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["trc", "dp", "tro", "tronic"])) {
    const { parseUADEFile: parseUADE_trc } = await __vitePreload(async () => {
      const { parseUADEFile: parseUADE_trc2 } = await import("./main-BbV5VyEH.js").then((n) => n.ji);
      return { parseUADEFile: parseUADE_trc2 };
    }, true ? __vite__mapDeps([0,1,2,3,4,5,6]) : void 0);
    return parseUADE_trc(buffer, originalFileName, prefs.uade ?? "enhanced", subsong, preScannedMeta);
  }
  if (matchesExt(filename, ["dl", "dl_deli"])) {
    const { isDaveLoweFormat, parseDaveLoweFile } = await __vitePreload(async () => {
      const { isDaveLoweFormat: isDaveLoweFormat2, parseDaveLoweFile: parseDaveLoweFile2 } = await import("./DaveLoweParser-kVqiosPY.js");
      return { isDaveLoweFormat: isDaveLoweFormat2, parseDaveLoweFile: parseDaveLoweFile2 };
    }, true ? __vite__mapDeps([103,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "daveLowe",
      ctx,
      (buf, name) => {
        const u8 = buf instanceof Uint8Array ? buf : new Uint8Array(buf);
        const ab = buf instanceof Uint8Array ? buf.buffer : buf;
        if (isDaveLoweFormat(u8)) return parseDaveLoweFile(ab, name);
        return null;
      },
      "DaveLoweParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["lme"])) {
    const { isLMEFormat, parseLMEFile } = await __vitePreload(async () => {
      const { isLMEFormat: isLMEFormat2, parseLMEFile: parseLMEFile2 } = await import("./LMEParser-DQgbIV_z.js");
      return { isLMEFormat: isLMEFormat2, parseLMEFile: parseLMEFile2 };
    }, true ? [] : void 0);
    return withNativeThenUADE(
      "lme",
      ctx,
      (buf, name) => {
        const ab = buf instanceof Uint8Array ? buf.buffer : buf;
        if (isLMEFormat(buf)) return parseLMEFile(ab, name);
        return null;
      },
      "LMEParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["md"])) {
    const { parseUADEFile } = await __vitePreload(async () => {
      const { parseUADEFile: parseUADEFile2 } = await import("./main-BbV5VyEH.js").then((n) => n.ji);
      return { parseUADEFile: parseUADEFile2 };
    }, true ? __vite__mapDeps([0,1,2,3,4,5,6]) : void 0);
    const song = await parseUADEFile(buffer, originalFileName, "enhanced", subsong, preScannedMeta);
    song.uadeEditableFileData = buffer.slice(0);
    song.uadeEditableFileName = originalFileName;
    return song;
  }
  if (matchesExt(filename, ["ml", "mso"])) {
    const { isMedleyFormat, parseMedleyFile } = await __vitePreload(async () => {
      const { isMedleyFormat: isMedleyFormat2, parseMedleyFile: parseMedleyFile2 } = await import("./MedleyParser-DV0TfPw9.js");
      return { isMedleyFormat: isMedleyFormat2, parseMedleyFile: parseMedleyFile2 };
    }, true ? [] : void 0);
    return withNativeThenUADE(
      "medley",
      ctx,
      (buf, name) => {
        const ab = buf instanceof Uint8Array ? buf.buffer : buf;
        if (isMedleyFormat(buf)) return parseMedleyFile(ab, name);
        return null;
      },
      "MedleyParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["mc", "mcr", "mco"])) {
    const { isMarkCookseyFormat, parseMarkCookseyFile } = await __vitePreload(async () => {
      const { isMarkCookseyFormat: isMarkCookseyFormat2, parseMarkCookseyFile: parseMarkCookseyFile2 } = await import("./MarkCookseyParser-Bgugdwlh.js");
      return { isMarkCookseyFormat: isMarkCookseyFormat2, parseMarkCookseyFile: parseMarkCookseyFile2 };
    }, true ? [] : void 0);
    const mcCtx = { ...ctx, originalFileName: toUADEPrefixName(originalFileName, ["mc", "mcr", "mco"]) };
    return withNativeThenUADE(
      "markCooksey",
      mcCtx,
      (buf, name) => {
        if (isMarkCookseyFormat(buf, name)) return parseMarkCookseyFile(buf, name);
        return null;
      },
      "MarkCookseyParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["jt", "mon_old"])) {
    const { isJeroenTelFormat, parseJeroenTelFile } = await __vitePreload(async () => {
      const { isJeroenTelFormat: isJeroenTelFormat2, parseJeroenTelFile: parseJeroenTelFile2 } = await import("./JeroenTelParser-BxTylhed.js");
      return { isJeroenTelFormat: isJeroenTelFormat2, parseJeroenTelFile: parseJeroenTelFile2 };
    }, true ? __vite__mapDeps([104,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "jeroenTel",
      ctx,
      (buf, name) => {
        const ab = buf instanceof Uint8Array ? buf.buffer : buf;
        if (isJeroenTelFormat(ab, name)) return parseJeroenTelFile(ab, name);
        return null;
      },
      "JeroenTelParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["qpa", "sqt", "qts", "4v"])) {
    const { parseUADEFile } = await __vitePreload(async () => {
      const { parseUADEFile: parseUADEFile2 } = await import("./main-BbV5VyEH.js").then((n) => n.ji);
      return { parseUADEFile: parseUADEFile2 };
    }, true ? __vite__mapDeps([0,1,2,3,4,5,6]) : void 0);
    const qFile = toUADEPrefixName(originalFileName, ["qpa", "sqt", "qts", "4v"]);
    const song = await parseUADEFile(buffer, qFile, "enhanced", subsong, preScannedMeta, companionFiles);
    song.uadeEditableFileData = buffer.slice(0);
    song.uadeEditableFileName = qFile;
    return song;
  }
  if (matchesExt(filename, ["sm", "sm1", "sm2", "sm3", "smpro"])) {
    const { isSoundMasterFormat, parseSoundMasterFile } = await __vitePreload(async () => {
      const { isSoundMasterFormat: isSoundMasterFormat2, parseSoundMasterFile: parseSoundMasterFile2 } = await import("./SoundMasterParser-BCK2oLBi.js");
      return { isSoundMasterFormat: isSoundMasterFormat2, parseSoundMasterFile: parseSoundMasterFile2 };
    }, true ? [] : void 0);
    return withNativeThenUADE(
      "soundMaster",
      ctx,
      (buf, name) => {
        const ab = buf instanceof Uint8Array ? buf.buffer : buf;
        if (isSoundMasterFormat(ab, name)) return parseSoundMasterFile(ab, name);
        return null;
      },
      "SoundMasterParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["sng"])) {
    const { isZoundMonitorFormat, parseZoundMonitorFile } = await __vitePreload(async () => {
      const { isZoundMonitorFormat: isZoundMonitorFormat2, parseZoundMonitorFile: parseZoundMonitorFile2 } = await import("./ZoundMonitorParser-DsMlDay5.js");
      return { isZoundMonitorFormat: isZoundMonitorFormat2, parseZoundMonitorFile: parseZoundMonitorFile2 };
    }, true ? __vite__mapDeps([105,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "zoundMonitor",
      ctx,
      (buf, name) => {
        if (isZoundMonitorFormat(buf, name)) return parseZoundMonitorFile(buf, name, companionFiles);
        return null;
      },
      "ZoundMonitorParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["fp"])) {
    const { isFuturePlayerFormat, parseFuturePlayerFile } = await __vitePreload(async () => {
      const { isFuturePlayerFormat: isFuturePlayerFormat2, parseFuturePlayerFile: parseFuturePlayerFile2 } = await import("./FuturePlayerParser-C5CitIHh.js");
      return { isFuturePlayerFormat: isFuturePlayerFormat2, parseFuturePlayerFile: parseFuturePlayerFile2 };
    }, true ? __vite__mapDeps([106,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "futurePlayer",
      ctx,
      (buf, name) => {
        if (isFuturePlayerFormat(buf)) return parseFuturePlayerFile(buf, name);
        return null;
      },
      "FuturePlayerParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["tcb"])) {
    const { isTCBTrackerFormat, parseTCBTrackerFile } = await __vitePreload(async () => {
      const { isTCBTrackerFormat: isTCBTrackerFormat2, parseTCBTrackerFile: parseTCBTrackerFile2 } = await import("./TCBTrackerParser-CgEzAn7h.js");
      return { isTCBTrackerFormat: isTCBTrackerFormat2, parseTCBTrackerFile: parseTCBTrackerFile2 };
    }, true ? __vite__mapDeps([107,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "tcbTracker",
      ctx,
      async (buf, name) => {
        if (isTCBTrackerFormat(buf)) return parseTCBTrackerFile(buf, name);
        return null;
      },
      "TCBTrackerParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["jpn", "jpnd", "jp", "jpo", "jpold"])) {
    const { isJasonPageFormat, parseJasonPageFile } = await __vitePreload(async () => {
      const { isJasonPageFormat: isJasonPageFormat2, parseJasonPageFile: parseJasonPageFile2 } = await import("./JasonPageParser-iY2Ju95V.js");
      return { isJasonPageFormat: isJasonPageFormat2, parseJasonPageFile: parseJasonPageFile2 };
    }, true ? __vite__mapDeps([108,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "jasonPage",
      ctx,
      (buf, name) => {
        const ab = buf instanceof Uint8Array ? buf.buffer : buf;
        if (isJasonPageFormat(ab, name)) return parseJasonPageFile(ab, name, companionFiles);
        return null;
      },
      "JasonPageParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["tme"])) {
    const { isTMEFormat, parseTMEFile } = await __vitePreload(async () => {
      const { isTMEFormat: isTMEFormat2, parseTMEFile: parseTMEFile2 } = await import("./TMEParser-CkpXyl5F.js");
      return { isTMEFormat: isTMEFormat2, parseTMEFile: parseTMEFile2 };
    }, true ? [] : void 0);
    return withNativeThenUADE(
      "tme",
      ctx,
      (buf, name) => {
        const ab = buf instanceof Uint8Array ? buf.buffer : buf;
        if (isTMEFormat(buf)) return parseTMEFile(ab, name);
        return null;
      },
      "TMEParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["dum"])) {
    const { isInfogramesFormat, parseInfogramesFile } = await __vitePreload(async () => {
      const { isInfogramesFormat: isInfogramesFormat2, parseInfogramesFile: parseInfogramesFile2 } = await import("./InfogramesParser-BfzBpwoC.js");
      return { isInfogramesFormat: isInfogramesFormat2, parseInfogramesFile: parseInfogramesFile2 };
    }, true ? [] : void 0);
    return withNativeThenUADE(
      "infogrames",
      ctx,
      (buf, name) => {
        const ab = buf instanceof Uint8Array ? buf.buffer : buf;
        if (isInfogramesFormat(buf)) return parseInfogramesFile(ab, name);
        return null;
      },
      "InfogramesParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["psa"])) {
    const { isPSAFormat, parsePSAFile } = await __vitePreload(async () => {
      const { isPSAFormat: isPSAFormat2, parsePSAFile: parsePSAFile2 } = await import("./PSAParser-B7vH_rPo.js");
      return { isPSAFormat: isPSAFormat2, parsePSAFile: parsePSAFile2 };
    }, true ? [] : void 0);
    return withNativeThenUADE(
      "psa",
      ctx,
      (buf, name) => {
        const ab = buf instanceof Uint8Array ? buf.buffer : buf;
        if (isPSAFormat(buf)) return parsePSAFile(ab, name);
        return null;
      },
      "PSAParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["mmdc"])) {
    const { isMMDCFormat, parseMMDCFile } = await __vitePreload(async () => {
      const { isMMDCFormat: isMMDCFormat2, parseMMDCFile: parseMMDCFile2 } = await import("./MMDCParser-DpSCs-OM.js");
      return { isMMDCFormat: isMMDCFormat2, parseMMDCFile: parseMMDCFile2 };
    }, true ? [] : void 0);
    return withNativeThenUADE(
      "mmdc",
      ctx,
      (buf, name) => {
        const ab = buf instanceof Uint8Array ? buf.buffer : buf;
        if (isMMDCFormat(buf)) return parseMMDCFile(ab, name);
        return null;
      },
      "MMDCParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["jpo", "jpold"])) {
    const { parseSteveTurnerFile } = await __vitePreload(async () => {
      const { parseSteveTurnerFile: parseSteveTurnerFile2 } = await import("./SteveTurnerParser-DtqwpMhs.js");
      return { parseSteveTurnerFile: parseSteveTurnerFile2 };
    }, true ? __vite__mapDeps([109,0,1,2,3,4,5,6]) : void 0);
    return withNativeDefault(
      "steveTurner",
      ctx,
      (buf, name) => parseSteveTurnerFile(buf, name)
    );
  }
  if (matchesExt(filename, ["tmk"])) {
    const { isTimeTrackerFormat, parseTimeTrackerFile } = await __vitePreload(async () => {
      const { isTimeTrackerFormat: isTimeTrackerFormat2, parseTimeTrackerFile: parseTimeTrackerFile2 } = await import("./TimeTrackerParser-7dHx0JoT.js");
      return { isTimeTrackerFormat: isTimeTrackerFormat2, parseTimeTrackerFile: parseTimeTrackerFile2 };
    }, true ? [] : void 0);
    return withNativeThenUADE(
      "timeTracker",
      ctx,
      (buf, name) => {
        const ab = buf instanceof Uint8Array ? buf.buffer : buf;
        if (isTimeTrackerFormat(buf)) return parseTimeTrackerFile(ab, name);
        return null;
      },
      "TimeTrackerParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["kris"])) {
    const { isKrisHatlelidFormat, parseKrisHatlelidFile } = await __vitePreload(async () => {
      const { isKrisHatlelidFormat: isKrisHatlelidFormat2, parseKrisHatlelidFile: parseKrisHatlelidFile2 } = await import("./KrisHatlelidParser-CktN4N79.js");
      return { isKrisHatlelidFormat: isKrisHatlelidFormat2, parseKrisHatlelidFile: parseKrisHatlelidFile2 };
    }, true ? __vite__mapDeps([110,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "krisHatlelid",
      ctx,
      (buf, name) => {
        const ab = buf instanceof Uint8Array ? buf.buffer : buf;
        if (isKrisHatlelidFormat(buf)) return parseKrisHatlelidFile(ab, name);
        return null;
      },
      "KrisHatlelidParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["cin"])) {
    const { isCinemawareFormat, parseCinemawareFile } = await __vitePreload(async () => {
      const { isCinemawareFormat: isCinemawareFormat2, parseCinemawareFile: parseCinemawareFile2 } = await import("./CinemawareParser-DJy8V_K_.js");
      return { isCinemawareFormat: isCinemawareFormat2, parseCinemawareFile: parseCinemawareFile2 };
    }, true ? [] : void 0);
    return withNativeThenUADE(
      "cinemaware",
      ctx,
      (buf, name) => {
        const ab = buf instanceof Uint8Array ? buf.buffer : buf;
        if (isCinemawareFormat(buf)) return parseCinemawareFile(ab, name);
        return null;
      },
      "CinemawareParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["ntp"])) {
    const { isNovoTradePackerFormat, parseNovoTradePackerFile } = await __vitePreload(async () => {
      const { isNovoTradePackerFormat: isNovoTradePackerFormat2, parseNovoTradePackerFile: parseNovoTradePackerFile2 } = await import("./NovoTradePackerParser-BcwFXFfo.js");
      return { isNovoTradePackerFormat: isNovoTradePackerFormat2, parseNovoTradePackerFile: parseNovoTradePackerFile2 };
    }, true ? __vite__mapDeps([111,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "novoTradePacker",
      ctx,
      (buf, name) => {
        if (isNovoTradePackerFormat(buf)) return parseNovoTradePackerFile(buf instanceof Uint8Array ? buf.buffer : buf, name);
        return null;
      },
      "NovoTradePackerParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["alp"])) {
    const { isAlcatrazPackerFormat, parseAlcatrazPackerFile } = await __vitePreload(async () => {
      const { isAlcatrazPackerFormat: isAlcatrazPackerFormat2, parseAlcatrazPackerFile: parseAlcatrazPackerFile2 } = await import("./AlcatrazPackerParser-9_HRU-FG.js");
      return { isAlcatrazPackerFormat: isAlcatrazPackerFormat2, parseAlcatrazPackerFile: parseAlcatrazPackerFile2 };
    }, true ? [] : void 0);
    return withNativeThenUADE(
      "alcatrazPacker",
      ctx,
      (buf, name) => {
        const ab = buf instanceof Uint8Array ? buf.buffer : buf;
        if (isAlcatrazPackerFormat(buf)) return parseAlcatrazPackerFile(ab, name);
        return null;
      },
      "AlcatrazPackerParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["uds"])) {
    const { parseUADEFile } = await __vitePreload(async () => {
      const { parseUADEFile: parseUADEFile2 } = await import("./main-BbV5VyEH.js").then((n) => n.ji);
      return { parseUADEFile: parseUADEFile2 };
    }, true ? __vite__mapDeps([0,1,2,3,4,5,6]) : void 0);
    const song = await parseUADEFile(buffer, originalFileName, "enhanced", subsong, preScannedMeta);
    song.uadeEditableFileData = buffer.slice(0);
    song.uadeEditableFileName = originalFileName;
    return song;
  }
  if (matchesExt(filename, ["sg"])) {
    const { isTomyTrackerFormat, parseTomyTrackerFile } = await __vitePreload(async () => {
      const { isTomyTrackerFormat: isTomyTrackerFormat2, parseTomyTrackerFile: parseTomyTrackerFile2 } = await import("./TomyTrackerParser-D7s7JhBE.js");
      return { isTomyTrackerFormat: isTomyTrackerFormat2, parseTomyTrackerFile: parseTomyTrackerFile2 };
    }, true ? [] : void 0);
    return withNativeThenUADE(
      "tomyTracker",
      ctx,
      (buf, name) => {
        if (isTomyTrackerFormat(buf)) return parseTomyTrackerFile(buf, name);
        return null;
      },
      "TomyTrackerParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["ex"])) {
    const { isFashionTrackerFormat, parseFashionTrackerFile } = await __vitePreload(async () => {
      const { isFashionTrackerFormat: isFashionTrackerFormat2, parseFashionTrackerFile: parseFashionTrackerFile2 } = await import("./FashionTrackerParser-D2Pq3KR7.js");
      return { isFashionTrackerFormat: isFashionTrackerFormat2, parseFashionTrackerFile: parseFashionTrackerFile2 };
    }, true ? __vite__mapDeps([112,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "fashionTracker",
      ctx,
      (buf, name) => {
        if (isFashionTrackerFormat(buf)) return parseFashionTrackerFile(buf instanceof Uint8Array ? buf.buffer : buf, name);
        return null;
      },
      "FashionTrackerParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["mms", "sfx20"])) {
    const { isMultiMediaSoundFormat, parseMultiMediaSoundFile } = await __vitePreload(async () => {
      const { isMultiMediaSoundFormat: isMultiMediaSoundFormat2, parseMultiMediaSoundFile: parseMultiMediaSoundFile2 } = await import("./MultiMediaSoundParser-CIj4N4pT.js");
      return { isMultiMediaSoundFormat: isMultiMediaSoundFormat2, parseMultiMediaSoundFile: parseMultiMediaSoundFile2 };
    }, true ? [] : void 0);
    return withNativeThenUADE(
      "multiMediaSound",
      ctx,
      (buf, name) => {
        const ab = buf instanceof Uint8Array ? buf.buffer : buf;
        if (isMultiMediaSoundFormat(buf)) return parseMultiMediaSoundFile(ab, name);
        return null;
      },
      "MultiMediaSoundParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["scr"])) {
    const { isSeanConranFormat, parseSeanConranFile } = await __vitePreload(async () => {
      const { isSeanConranFormat: isSeanConranFormat2, parseSeanConranFile: parseSeanConranFile2 } = await import("./SeanConranParser-6mlCgq8D.js");
      return { isSeanConranFormat: isSeanConranFormat2, parseSeanConranFile: parseSeanConranFile2 };
    }, true ? [] : void 0);
    return withNativeThenUADE(
      "seanConran",
      ctx,
      (buf, name) => {
        const u8 = buf instanceof Uint8Array ? buf : new Uint8Array(buf);
        const ab = buf instanceof Uint8Array ? buf.buffer : buf;
        if (isSeanConranFormat(u8)) return parseSeanConranFile(ab, name);
        return null;
      },
      "SeanConranParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["thm"])) {
    const { isThomasHermannFormat, parseThomasHermannFile } = await __vitePreload(async () => {
      const { isThomasHermannFormat: isThomasHermannFormat2, parseThomasHermannFile: parseThomasHermannFile2 } = await import("./ThomasHermannParser-cNwmnaZM.js");
      return { isThomasHermannFormat: isThomasHermannFormat2, parseThomasHermannFile: parseThomasHermannFile2 };
    }, true ? __vite__mapDeps([113,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "thomasHermann",
      ctx,
      (buf, name) => {
        const u8 = buf instanceof Uint8Array ? buf : new Uint8Array(buf);
        if (isThomasHermannFormat(u8)) return parseThomasHermannFile(buf instanceof Uint8Array ? buf.buffer : buf, name);
        return null;
      },
      "ThomasHermannParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["tits"])) {
    const { parseUADEFile } = await __vitePreload(async () => {
      const { parseUADEFile: parseUADEFile2 } = await import("./main-BbV5VyEH.js").then((n) => n.ji);
      return { parseUADEFile: parseUADEFile2 };
    }, true ? __vite__mapDeps([0,1,2,3,4,5,6]) : void 0);
    const song = await parseUADEFile(buffer, originalFileName, "enhanced", subsong, preScannedMeta);
    song.uadeEditableFileData = buffer.slice(0);
    song.uadeEditableFileName = originalFileName;
    return song;
  }
  if (matchesExt(filename, ["kh"])) {
    const { isKrisHatlelidFormat, parseKrisHatlelidFile } = await __vitePreload(async () => {
      const { isKrisHatlelidFormat: isKrisHatlelidFormat2, parseKrisHatlelidFile: parseKrisHatlelidFile2 } = await import("./KrisHatlelidParser-CktN4N79.js");
      return { isKrisHatlelidFormat: isKrisHatlelidFormat2, parseKrisHatlelidFile: parseKrisHatlelidFile2 };
    }, true ? __vite__mapDeps([110,0,1,2,3,4,5,6]) : void 0);
    const khCtx = { ...ctx, originalFileName: toUADEPrefixName(originalFileName, ["kh"]) };
    return withNativeThenUADE(
      "krisHatlelid",
      khCtx,
      (buf, name) => {
        if (isKrisHatlelidFormat(buf)) return parseKrisHatlelidFile(buf, name);
        return null;
      },
      "KrisHatlelidParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["two"])) {
    const { isNTSPFormat, parseNTSPFile } = await __vitePreload(async () => {
      const { isNTSPFormat: isNTSPFormat2, parseNTSPFile: parseNTSPFile2 } = await import("./NTSPParser-toyzc6Tm.js");
      return { isNTSPFormat: isNTSPFormat2, parseNTSPFile: parseNTSPFile2 };
    }, true ? [] : void 0);
    return withNativeThenUADE(
      "ntsp",
      ctx,
      (buf, name) => {
        if (isNTSPFormat(buf)) return parseNTSPFile(buf, name);
        return null;
      },
      "NTSPParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["mus", "ufo"])) {
    const { parseUADEFile } = await __vitePreload(async () => {
      const { parseUADEFile: parseUADEFile2 } = await import("./main-BbV5VyEH.js").then((n) => n.ji);
      return { parseUADEFile: parseUADEFile2 };
    }, true ? __vite__mapDeps([0,1,2,3,4,5,6]) : void 0);
    const musFile = toUADEPrefixName(originalFileName, ["mus", "ufo"]);
    const song = await parseUADEFile(buffer, musFile, "enhanced", subsong, preScannedMeta);
    song.uadeEditableFileData = buffer.slice(0);
    song.uadeEditableFileName = musFile;
    return song;
  }
  if (matchesExt(filename, ["mosh"])) {
    const { isMoshPackerFormat, parseMoshPackerFile } = await __vitePreload(async () => {
      const { isMoshPackerFormat: isMoshPackerFormat2, parseMoshPackerFile: parseMoshPackerFile2 } = await import("./MoshPackerParser-CXU-aWSR.js");
      return { isMoshPackerFormat: isMoshPackerFormat2, parseMoshPackerFile: parseMoshPackerFile2 };
    }, true ? [] : void 0);
    return withNativeThenUADE(
      "moshPacker",
      ctx,
      (buf, name) => {
        if (isMoshPackerFormat(buf)) return parseMoshPackerFile(buf, name);
        return null;
      },
      "MoshPackerParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["mug"])) {
    const { parseDigitalMugicianFile } = await __vitePreload(async () => {
      const { parseDigitalMugicianFile: parseDigitalMugicianFile2 } = await import("./DigitalMugicianParser-DlVBZ5dm.js");
      return { parseDigitalMugicianFile: parseDigitalMugicianFile2 };
    }, true ? __vite__mapDeps([52,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE("mugician", ctx, (buf, name) => parseDigitalMugicianFile(buf, name), "DigitalMugicianParser", { injectUADE: true });
  }
  if (matchesExt(filename, ["mug2"])) {
    const { parseDigitalMugicianFile } = await __vitePreload(async () => {
      const { parseDigitalMugicianFile: parseDigitalMugicianFile2 } = await import("./DigitalMugicianParser-DlVBZ5dm.js");
      return { parseDigitalMugicianFile: parseDigitalMugicianFile2 };
    }, true ? __vite__mapDeps([52,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE("mugician", ctx, (buf, name) => parseDigitalMugicianFile(buf, name), "DigitalMugicianParser", { injectUADE: true });
  }
  if (matchesExt(filename, ["core"])) {
    const coreCtx = { ...ctx, originalFileName: toUADEPrefixName(originalFileName, ["core"]) };
    const { isCoreDesignFormat, parseCoreDesignFile } = await __vitePreload(async () => {
      const { isCoreDesignFormat: isCoreDesignFormat2, parseCoreDesignFile: parseCoreDesignFile2 } = await import("./CoreDesignParser-O97ycm3Z.js");
      return { isCoreDesignFormat: isCoreDesignFormat2, parseCoreDesignFile: parseCoreDesignFile2 };
    }, true ? __vite__mapDeps([114,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "coreDesign",
      coreCtx,
      (buf, name) => {
        const ab = buf instanceof Uint8Array ? buf.buffer : buf;
        if (isCoreDesignFormat(ab)) return parseCoreDesignFile(ab, name);
        return null;
      },
      "CoreDesignParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["jmf"])) {
    const { isJankoMrsicFlogelFormat, parseJankoMrsicFlogelFile } = await __vitePreload(async () => {
      const { isJankoMrsicFlogelFormat: isJankoMrsicFlogelFormat2, parseJankoMrsicFlogelFile: parseJankoMrsicFlogelFile2 } = await import("./JankoMrsicFlogelParser-BSa6PMQh.js");
      return { isJankoMrsicFlogelFormat: isJankoMrsicFlogelFormat2, parseJankoMrsicFlogelFile: parseJankoMrsicFlogelFile2 };
    }, true ? [] : void 0);
    const jmfCtx = { ...ctx, originalFileName: toUADEPrefixName(originalFileName, ["jmf"]) };
    return withNativeThenUADE(
      "jankoMrsicFlogel",
      jmfCtx,
      (buf, name) => {
        if (isJankoMrsicFlogelFormat(buf)) return parseJankoMrsicFlogelFile(buf, name);
        return null;
      },
      "JankoMrsicFlogelParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["jd"])) {
    const { parseUADEFile } = await __vitePreload(async () => {
      const { parseUADEFile: parseUADEFile2 } = await import("./main-BbV5VyEH.js").then((n) => n.ji);
      return { parseUADEFile: parseUADEFile2 };
    }, true ? __vite__mapDeps([0,1,2,3,4,5,6]) : void 0);
    return parseUADEFile(buffer, toUADEPrefixName(originalFileName, ["jd"]), prefs.uade ?? "enhanced", subsong, preScannedMeta);
  }
  if (matchesExt(filename, ["sjs"])) {
    const { isSoundPlayerFormat, parseSoundPlayerFile } = await __vitePreload(async () => {
      const { isSoundPlayerFormat: isSoundPlayerFormat2, parseSoundPlayerFile: parseSoundPlayerFile2 } = await import("./SoundPlayerParser-Cxb-R9Ob.js");
      return { isSoundPlayerFormat: isSoundPlayerFormat2, parseSoundPlayerFile: parseSoundPlayerFile2 };
    }, true ? __vite__mapDeps([115,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "soundPlayer",
      ctx,
      (buf, name) => {
        if (isSoundPlayerFormat(buf)) return parseSoundPlayerFile(buf, name);
        return null;
      },
      "SoundPlayerParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["npp"])) {
    const { isNickPellingPackerFormat, parseNickPellingPackerFile } = await __vitePreload(async () => {
      const { isNickPellingPackerFormat: isNickPellingPackerFormat2, parseNickPellingPackerFile: parseNickPellingPackerFile2 } = await import("./NickPellingPackerParser-BFm9SVMQ.js");
      return { isNickPellingPackerFormat: isNickPellingPackerFormat2, parseNickPellingPackerFile: parseNickPellingPackerFile2 };
    }, true ? __vite__mapDeps([116,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "nickPellingPacker",
      ctx,
      (buf, name) => {
        if (isNickPellingPackerFormat(buf)) return parseNickPellingPackerFile(buf, name);
        return null;
      },
      "NickPellingPackerParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["pvp"])) {
    const { parseUADEFile } = await __vitePreload(async () => {
      const { parseUADEFile: parseUADEFile2 } = await import("./main-BbV5VyEH.js").then((n) => n.ji);
      return { parseUADEFile: parseUADEFile2 };
    }, true ? __vite__mapDeps([0,1,2,3,4,5,6]) : void 0);
    return parseUADEFile(buffer, toUADEPrefixName(originalFileName, ["pvp"]), prefs.uade ?? "enhanced", subsong, preScannedMeta);
  }
  if (matchesExt(filename, ["wb"])) {
    const { isWallyBebenFormat, parseWallyBebenFile } = await __vitePreload(async () => {
      const { isWallyBebenFormat: isWallyBebenFormat2, parseWallyBebenFile: parseWallyBebenFile2 } = await import("./WallyBebenParser-DxIKdBW6.js");
      return { isWallyBebenFormat: isWallyBebenFormat2, parseWallyBebenFile: parseWallyBebenFile2 };
    }, true ? __vite__mapDeps([117,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "wallyBeben",
      ctx,
      (buf, name) => {
        if (isWallyBebenFormat(buf)) return parseWallyBebenFile(buf instanceof Uint8Array ? buf.buffer : buf, name);
        return null;
      },
      "WallyBebenParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["sb"])) {
    const { isSteveBarrettFormat, parseSteveBarrettFile } = await __vitePreload(async () => {
      const { isSteveBarrettFormat: isSteveBarrettFormat2, parseSteveBarrettFile: parseSteveBarrettFile2 } = await import("./SteveBarrettParser-Cng-SqGV.js");
      return { isSteveBarrettFormat: isSteveBarrettFormat2, parseSteveBarrettFile: parseSteveBarrettFile2 };
    }, true ? [] : void 0);
    return withNativeThenUADE(
      "steveBarrett",
      ctx,
      (buf, name) => {
        const ab = buf instanceof Uint8Array ? buf.buffer : buf;
        if (isSteveBarrettFormat(buf)) return parseSteveBarrettFile(ab, name);
        return null;
      },
      "SteveBarrettParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["snk"])) {
    const { parseUADEFile } = await __vitePreload(async () => {
      const { parseUADEFile: parseUADEFile2 } = await import("./main-BbV5VyEH.js").then((n) => n.ji);
      return { parseUADEFile: parseUADEFile2 };
    }, true ? __vite__mapDeps([0,1,2,3,4,5,6]) : void 0);
    const song = await parseUADEFile(buffer, originalFileName, "enhanced", subsong, preScannedMeta);
    song.uadeEditableFileData = buffer.slice(0);
    song.uadeEditableFileName = originalFileName;
    return song;
  }
  if (matchesExt(filename, ["dsr"])) {
    const { isDesireFormat, parseDesireFile } = await __vitePreload(async () => {
      const { isDesireFormat: isDesireFormat2, parseDesireFile: parseDesireFile2 } = await import("./DesireParser-D0wOvieS.js");
      return { isDesireFormat: isDesireFormat2, parseDesireFile: parseDesireFile2 };
    }, true ? __vite__mapDeps([118,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "desire",
      ctx,
      (buf, name) => {
        if (isDesireFormat(buf)) return parseDesireFile(buf instanceof Uint8Array ? buf.buffer : buf, name);
        return null;
      },
      "DesireParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["dln"])) {
    const { parseUADEFile } = await __vitePreload(async () => {
      const { parseUADEFile: parseUADEFile2 } = await import("./main-BbV5VyEH.js").then((n) => n.ji);
      return { parseUADEFile: parseUADEFile2 };
    }, true ? __vite__mapDeps([0,1,2,3,4,5,6]) : void 0);
    return parseUADEFile(buffer, toUADEPrefixName(originalFileName, ["dln"]), prefs.uade ?? "enhanced", subsong, preScannedMeta);
  }
  if (matchesExt(filename, ["avp", "mw"])) {
    const { isMartinWalkerFormat, parseMartinWalkerFile } = await __vitePreload(async () => {
      const { isMartinWalkerFormat: isMartinWalkerFormat2, parseMartinWalkerFile: parseMartinWalkerFile2 } = await import("./MartinWalkerParser-ZR0uJogW.js");
      return { isMartinWalkerFormat: isMartinWalkerFormat2, parseMartinWalkerFile: parseMartinWalkerFile2 };
    }, true ? __vite__mapDeps([119,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "martinWalker",
      ctx,
      (buf, name) => {
        if (isMartinWalkerFormat(buf)) return parseMartinWalkerFile(buf, name);
        return null;
      },
      "MartinWalkerParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["ps"])) {
    const { isPaulShieldsFormat, parsePaulShieldsFile } = await __vitePreload(async () => {
      const { isPaulShieldsFormat: isPaulShieldsFormat2, parsePaulShieldsFile: parsePaulShieldsFile2 } = await import("./PaulShieldsParser-Ca-bhh1B.js");
      return { isPaulShieldsFormat: isPaulShieldsFormat2, parsePaulShieldsFile: parsePaulShieldsFile2 };
    }, true ? __vite__mapDeps([120,0,1,2,3,4,5,6]) : void 0);
    if (isPaulShieldsFormat(buffer)) {
      const result = parsePaulShieldsFile(buffer, originalFileName);
      if (!result.uadeEditableFileData) {
        result.uadeEditableFileData = buffer.slice(0);
        result.uadeEditableFileName = toUADEPrefixName(originalFileName, ["ps"]);
      }
      return result;
    }
    return callUADE(ctx);
  }
  if (matchesExt(filename, ["dat"])) {
    const { isPaulRobothamFormat, parsePaulRobothamFile } = await __vitePreload(async () => {
      const { isPaulRobothamFormat: isPaulRobothamFormat2, parsePaulRobothamFile: parsePaulRobothamFile2 } = await import("./PaulRobothamParser-BKP5I462.js");
      return { isPaulRobothamFormat: isPaulRobothamFormat2, parsePaulRobothamFile: parsePaulRobothamFile2 };
    }, true ? __vite__mapDeps([121,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "paulRobotham",
      ctx,
      (buf, name) => {
        if (isPaulRobothamFormat(buf)) return parsePaulRobothamFile(buf, name);
        return null;
      },
      "PaulRobothamParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["pap"])) {
    const { isPierreAdaneFormat, parsePierreAdaneFile } = await __vitePreload(async () => {
      const { isPierreAdaneFormat: isPierreAdaneFormat2, parsePierreAdaneFile: parsePierreAdaneFile2 } = await import("./PierreAdaneParser-BGmWgZuG.js");
      return { isPierreAdaneFormat: isPierreAdaneFormat2, parsePierreAdaneFile: parsePierreAdaneFile2 };
    }, true ? [] : void 0);
    return withNativeThenUADE(
      "pierreAdane",
      ctx,
      (buf, name) => {
        if (isPierreAdaneFormat(buf)) return parsePierreAdaneFile(buf, name);
        return null;
      },
      "PierreAdaneParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["hot"])) {
    const { parseUADEFile } = await __vitePreload(async () => {
      const { parseUADEFile: parseUADEFile2 } = await import("./main-BbV5VyEH.js").then((n) => n.ji);
      return { parseUADEFile: parseUADEFile2 };
    }, true ? __vite__mapDeps([0,1,2,3,4,5,6]) : void 0);
    return parseUADEFile(buffer, toUADEPrefixName(originalFileName, ["hot"]), prefs.uade ?? "enhanced", subsong, preScannedMeta);
  }
  if (matchesExt(filename, ["bye"])) {
    const { isAndrewPartonFormat, parseAndrewPartonFile } = await __vitePreload(async () => {
      const { isAndrewPartonFormat: isAndrewPartonFormat2, parseAndrewPartonFile: parseAndrewPartonFile2 } = await import("./AndrewPartonParser-Cr3Dooet.js");
      return { isAndrewPartonFormat: isAndrewPartonFormat2, parseAndrewPartonFile: parseAndrewPartonFile2 };
    }, true ? __vite__mapDeps([122,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "andrewParton",
      ctx,
      async (buf, name) => {
        if (isAndrewPartonFormat(buf, name)) return await parseAndrewPartonFile(buf, name);
        return null;
      },
      "AndrewPartonParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["cm", "rk", "rkb"])) {
    const { isCustomMadeFormat, parseCustomMadeFile } = await __vitePreload(async () => {
      const { isCustomMadeFormat: isCustomMadeFormat2, parseCustomMadeFile: parseCustomMadeFile2 } = await import("./CustomMadeParser-DsBfPnzd.js");
      return { isCustomMadeFormat: isCustomMadeFormat2, parseCustomMadeFile: parseCustomMadeFile2 };
    }, true ? [] : void 0);
    return withNativeThenUADE(
      "customMade",
      ctx,
      async (buf, name) => {
        if (isCustomMadeFormat(buf, name)) return await parseCustomMadeFile(buf, name);
        return null;
      },
      "CustomMadeParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["bds"])) {
    const { isBenDaglishSIDFormat, parseBenDaglishSIDFile } = await __vitePreload(async () => {
      const { isBenDaglishSIDFormat: isBenDaglishSIDFormat2, parseBenDaglishSIDFile: parseBenDaglishSIDFile2 } = await import("./BenDaglishSIDParser-CJnZADsP.js");
      return { isBenDaglishSIDFormat: isBenDaglishSIDFormat2, parseBenDaglishSIDFile: parseBenDaglishSIDFile2 };
    }, true ? [] : void 0);
    return withNativeThenUADE(
      "benDaglishSID",
      ctx,
      async (buf, name) => {
        if (isBenDaglishSIDFormat(buf, name)) return parseBenDaglishSIDFile(buf, name);
        return null;
      },
      "BenDaglishSIDParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["dsc"])) {
    const { isDscFormat, parseDscFile } = await __vitePreload(async () => {
      const { isDscFormat: isDscFormat2, parseDscFile: parseDscFile2 } = await import("./DigitalSonixChromeParser-D5cpGDHk.js");
      return { isDscFormat: isDscFormat2, parseDscFile: parseDscFile2 };
    }, true ? __vite__mapDeps([123,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "digitalSonixChrome",
      ctx,
      (buf, name) => {
        if (isDscFormat(buf)) return parseDscFile(buf, name);
        return null;
      },
      "DigitalSonixChromeParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["jo"])) {
    const { parseUADEFile } = await __vitePreload(async () => {
      const { parseUADEFile: parseUADEFile2 } = await import("./main-BbV5VyEH.js").then((n) => n.ji);
      return { parseUADEFile: parseUADEFile2 };
    }, true ? __vite__mapDeps([0,1,2,3,4,5,6]) : void 0);
    const song = await parseUADEFile(buffer, originalFileName, "enhanced", subsong, preScannedMeta);
    song.uadeEditableFileData = buffer.slice(0);
    song.uadeEditableFileName = originalFileName;
    return song;
  }
  if (matchesExt(filename, ["kim"])) {
    const { isKimChristensenFormat, parseKimChristensenFile } = await __vitePreload(async () => {
      const { isKimChristensenFormat: isKimChristensenFormat2, parseKimChristensenFile: parseKimChristensenFile2 } = await import("./KimChristensenParser-BX2Yl8Rp.js");
      return { isKimChristensenFormat: isKimChristensenFormat2, parseKimChristensenFile: parseKimChristensenFile2 };
    }, true ? __vite__mapDeps([124,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "kimChristensen",
      ctx,
      (buf, name) => {
        if (isKimChristensenFormat(buf)) return parseKimChristensenFile(buf instanceof Uint8Array ? buf.buffer : buf, name);
        return null;
      },
      "KimChristensenParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["ash"])) {
    const { isAshleyHoggFormat, parseAshleyHoggFile } = await __vitePreload(async () => {
      const { isAshleyHoggFormat: isAshleyHoggFormat2, parseAshleyHoggFile: parseAshleyHoggFile2 } = await import("./AshleyHoggParser-yHRHZP8w.js");
      return { isAshleyHoggFormat: isAshleyHoggFormat2, parseAshleyHoggFile: parseAshleyHoggFile2 };
    }, true ? __vite__mapDeps([125,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "ashleyHogg",
      ctx,
      (buf, name) => {
        if (isAshleyHoggFormat(buf)) return parseAshleyHoggFile(buf, name);
        return null;
      },
      "AshleyHoggParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["adpcm"])) {
    const { isADPCMmonoFormat, parseADPCMmonoFile } = await __vitePreload(async () => {
      const { isADPCMmonoFormat: isADPCMmonoFormat2, parseADPCMmonoFile: parseADPCMmonoFile2 } = await import("./ADPCMmonoParser-hzOUYt8F.js");
      return { isADPCMmonoFormat: isADPCMmonoFormat2, parseADPCMmonoFile: parseADPCMmonoFile2 };
    }, true ? [] : void 0);
    return withNativeThenUADE(
      "adpcmMono",
      ctx,
      (buf, name) => {
        if (isADPCMmonoFormat(buf, name)) return parseADPCMmonoFile(buf, name);
        return null;
      },
      "ADPCMmonoParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["js"])) {
    const { isJanneSalmijarviFormat, parseJanneSalmijarviFile } = await __vitePreload(async () => {
      const { isJanneSalmijarviFormat: isJanneSalmijarviFormat2, parseJanneSalmijarviFile: parseJanneSalmijarviFile2 } = await import("./JanneSalmijarviParser-DbO0Ktwz.js");
      return { isJanneSalmijarviFormat: isJanneSalmijarviFormat2, parseJanneSalmijarviFile: parseJanneSalmijarviFile2 };
    }, true ? __vite__mapDeps([126,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "janneSalmijarvi",
      ctx,
      (buf, name) => {
        if (isJanneSalmijarviFormat(buf)) return parseJanneSalmijarviFile(buf, name);
        return null;
      },
      "JanneSalmijarviParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["hip7", "s7g"])) {
    const { isJochenHippel7VFormat, parseJochenHippel7VFile } = await __vitePreload(async () => {
      const { isJochenHippel7VFormat: isJochenHippel7VFormat2, parseJochenHippel7VFile: parseJochenHippel7VFile2 } = await import("./JochenHippel7VParser-JwiND2SC.js");
      return { isJochenHippel7VFormat: isJochenHippel7VFormat2, parseJochenHippel7VFile: parseJochenHippel7VFile2 };
    }, true ? [] : void 0);
    return withNativeThenUADE(
      "jochenHippel7V",
      ctx,
      (buf, name) => {
        if (isJochenHippel7VFormat(buf)) return parseJochenHippel7VFile(buf, name);
        return null;
      },
      "JochenHippel7VParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["sog", "hst", "hip"])) {
    const { isJochenHippelSTFormat, parseJochenHippelSTFile } = await __vitePreload(async () => {
      const { isJochenHippelSTFormat: isJochenHippelSTFormat2, parseJochenHippelSTFile: parseJochenHippelSTFile2 } = await import("./JochenHippelSTParser-BPGIIdCV.js");
      return { isJochenHippelSTFormat: isJochenHippelSTFormat2, parseJochenHippelSTFile: parseJochenHippelSTFile2 };
    }, true ? [] : void 0);
    return withNativeThenUADE(
      "jochenHippelST",
      ctx,
      (buf, name) => {
        if (isJochenHippelSTFormat(buf)) return parseJochenHippelSTFile(buf, name);
        return null;
      },
      "JochenHippelSTParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["mxtx"])) {
    const { parseUADEFile } = await __vitePreload(async () => {
      const { parseUADEFile: parseUADEFile2 } = await import("./main-BbV5VyEH.js").then((n) => n.ji);
      return { parseUADEFile: parseUADEFile2 };
    }, true ? __vite__mapDeps([0,1,2,3,4,5,6]) : void 0);
    return parseUADEFile(buffer, toUADEPrefixName(originalFileName, ["mxtx"]), prefs.uade ?? "enhanced", subsong, preScannedMeta);
  }
  if (matchesExt(filename, ["adsc", "mod_adsc4"])) {
    const { parseStartrekkerAMFile } = await __vitePreload(async () => {
      const { parseStartrekkerAMFile: parseStartrekkerAMFile2 } = await import("./StartrekkerAMParser-By5VEfa4.js");
      return { parseStartrekkerAMFile: parseStartrekkerAMFile2 };
    }, true ? __vite__mapDeps([127,0,1,2,3,4,5,6]) : void 0);
    let ntBuffer = null;
    if (companionFiles && companionFiles.size > 0) {
      const candidates = [
        `${originalFileName}.nt`,
        `${originalFileName.replace(/\.(adsc|mod)$/i, "")}.nt`
      ];
      for (const key of candidates) {
        const found = companionFiles.get(key) ?? companionFiles.get(key.toLowerCase());
        if (found) {
          ntBuffer = found;
          break;
        }
      }
      if (!ntBuffer) {
        for (const [key, val] of companionFiles.entries()) {
          if (key.toLowerCase().endsWith(".nt")) {
            ntBuffer = val;
            break;
          }
        }
      }
    }
    let modAB;
    if (ArrayBuffer.isView(buffer)) {
      const view = buffer;
      modAB = view.buffer.slice(view.byteOffset, view.byteOffset + view.byteLength);
    } else {
      modAB = buffer;
    }
    return parseStartrekkerAMFile(modAB, originalFileName, ntBuffer);
  }
  if (matchesExt(filename, ["max"])) {
    const { isMaximumEffectFormat, parseMaximumEffectFile } = await __vitePreload(async () => {
      const { isMaximumEffectFormat: isMaximumEffectFormat2, parseMaximumEffectFile: parseMaximumEffectFile2 } = await import("./MaximumEffectParser-DiJbXk--.js");
      return { isMaximumEffectFormat: isMaximumEffectFormat2, parseMaximumEffectFile: parseMaximumEffectFile2 };
    }, true ? __vite__mapDeps([128,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "maximumEffect",
      ctx,
      (buf, name) => {
        if (isMaximumEffectFormat(buf)) return parseMaximumEffectFile(buf, name);
        return null;
      },
      "MaximumEffectParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["midi"])) {
    const { isMIDILoricielFormat, parseMIDILoricielFile } = await __vitePreload(async () => {
      const { isMIDILoricielFormat: isMIDILoricielFormat2, parseMIDILoricielFile: parseMIDILoricielFile2 } = await import("./MIDILoricielParser-DmKZ-DAT.js");
      return { isMIDILoricielFormat: isMIDILoricielFormat2, parseMIDILoricielFile: parseMIDILoricielFile2 };
    }, true ? [] : void 0);
    return withNativeThenUADE(
      "midiLoriciel",
      ctx,
      (buf, name) => {
        if (isMIDILoricielFormat(buf)) return parseMIDILoricielFile(buf, name);
        return null;
      },
      "MIDILoricielParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["one"])) {
    const { isOnEscapeeFormat, parseOnEscapeeFile } = await __vitePreload(async () => {
      const { isOnEscapeeFormat: isOnEscapeeFormat2, parseOnEscapeeFile: parseOnEscapeeFile2 } = await import("./OnEscapeeParser-D522J176.js");
      return { isOnEscapeeFormat: isOnEscapeeFormat2, parseOnEscapeeFile: parseOnEscapeeFile2 };
    }, true ? [] : void 0);
    return withNativeThenUADE(
      "onEscapee",
      ctx,
      (buf, name) => {
        if (isOnEscapeeFormat(buf)) return parseOnEscapeeFile(buf, name);
        return null;
      },
      "OnEscapeeParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["pat"])) {
    const { isPaulTongeFormat, parsePaulTongeFile } = await __vitePreload(async () => {
      const { isPaulTongeFormat: isPaulTongeFormat2, parsePaulTongeFile: parsePaulTongeFile2 } = await import("./PaulTongeParser-B7DYXn39.js");
      return { isPaulTongeFormat: isPaulTongeFormat2, parsePaulTongeFile: parsePaulTongeFile2 };
    }, true ? __vite__mapDeps([129,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "paulTonge",
      ctx,
      (buf, name) => {
        if (isPaulTongeFormat(buf)) return parsePaulTongeFile(buf, name);
        return null;
      },
      "PaulTongeParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["rho"])) {
    const { parseUADEFile } = await __vitePreload(async () => {
      const { parseUADEFile: parseUADEFile2 } = await import("./main-BbV5VyEH.js").then((n) => n.ji);
      return { parseUADEFile: parseUADEFile2 };
    }, true ? __vite__mapDeps([0,1,2,3,4,5,6]) : void 0);
    return parseUADEFile(buffer, toUADEPrefixName(originalFileName, ["rho"]), prefs.uade ?? "enhanced", subsong, preScannedMeta);
  }
  if (matchesExt(filename, ["rh"])) {
    const { isRobHubbardFormat, parseRobHubbardFile } = await __vitePreload(async () => {
      const { isRobHubbardFormat: isRobHubbardFormat2, parseRobHubbardFile: parseRobHubbardFile2 } = await import("./RobHubbardParser-DczqYvyl.js");
      return { isRobHubbardFormat: isRobHubbardFormat2, parseRobHubbardFile: parseRobHubbardFile2 };
    }, true ? __vite__mapDeps([49,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "robHubbard",
      ctx,
      async (buf, name) => {
        if (isRobHubbardFormat(buf, name)) return parseRobHubbardFile(buf, name);
        return null;
      },
      "RobHubbardParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["amc"])) {
    const { parseUADEFile: parseUADE_amc } = await __vitePreload(async () => {
      const { parseUADEFile: parseUADE_amc2 } = await import("./main-BbV5VyEH.js").then((n) => n.ji);
      return { parseUADEFile: parseUADE_amc2 };
    }, true ? __vite__mapDeps([0,1,2,3,4,5,6]) : void 0);
    return parseUADE_amc(buffer, toUADEPrefixName(originalFileName, ["amc"]), prefs.uade ?? "enhanced", subsong, preScannedMeta);
  }
  if (matchesExt(filename, ["spl"])) {
    const wtCtx = { ...ctx, originalFileName: toUADEPrefixName(originalFileName, ["spl"]) };
    const { isWantedTeamDaveLoweFormat, parseWantedTeamDaveLoweFile } = await __vitePreload(async () => {
      const { isWantedTeamDaveLoweFormat: isWantedTeamDaveLoweFormat2, parseWantedTeamDaveLoweFile: parseWantedTeamDaveLoweFile2 } = await import("./WantedTeamDaveLoweParser-CzkVhSGq.js");
      return { isWantedTeamDaveLoweFormat: isWantedTeamDaveLoweFormat2, parseWantedTeamDaveLoweFile: parseWantedTeamDaveLoweFile2 };
    }, true ? __vite__mapDeps([130,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "soprol",
      wtCtx,
      (buf, name) => {
        if (isWantedTeamDaveLoweFormat(buf)) return parseWantedTeamDaveLoweFile(buf instanceof Uint8Array ? buf.buffer : buf, name);
        return null;
      },
      "WantedTeamDaveLoweParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["riff"])) {
    const wtCtx = { ...ctx, originalFileName: toUADEPrefixName(originalFileName, ["riff"]) };
    const { isWantedTeamDaveLoweFormat, parseWantedTeamDaveLoweFile } = await __vitePreload(async () => {
      const { isWantedTeamDaveLoweFormat: isWantedTeamDaveLoweFormat2, parseWantedTeamDaveLoweFile: parseWantedTeamDaveLoweFile2 } = await import("./WantedTeamDaveLoweParser-CzkVhSGq.js");
      return { isWantedTeamDaveLoweFormat: isWantedTeamDaveLoweFormat2, parseWantedTeamDaveLoweFile: parseWantedTeamDaveLoweFile2 };
    }, true ? __vite__mapDeps([130,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "riffRaff",
      wtCtx,
      (buf, name) => {
        if (isWantedTeamDaveLoweFormat(buf)) return parseWantedTeamDaveLoweFile(buf instanceof Uint8Array ? buf.buffer : buf, name);
        return null;
      },
      "WantedTeamDaveLoweParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["hd"])) {
    const wtCtx = { ...ctx, originalFileName: toUADEPrefixName(originalFileName, ["hd"]) };
    const { isWantedTeamDaveLoweFormat, parseWantedTeamDaveLoweFile } = await __vitePreload(async () => {
      const { isWantedTeamDaveLoweFormat: isWantedTeamDaveLoweFormat2, parseWantedTeamDaveLoweFile: parseWantedTeamDaveLoweFile2 } = await import("./WantedTeamDaveLoweParser-CzkVhSGq.js");
      return { isWantedTeamDaveLoweFormat: isWantedTeamDaveLoweFormat2, parseWantedTeamDaveLoweFile: parseWantedTeamDaveLoweFile2 };
    }, true ? __vite__mapDeps([130,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "howieDavies",
      wtCtx,
      (buf, name) => {
        if (isWantedTeamDaveLoweFormat(buf)) return parseWantedTeamDaveLoweFile(buf instanceof Uint8Array ? buf.buffer : buf, name);
        return null;
      },
      "WantedTeamDaveLoweParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["bss"])) {
    const wtCtx = { ...ctx, originalFileName: toUADEPrefixName(originalFileName, ["bss"]) };
    const { isWantedTeamDaveLoweFormat, parseWantedTeamDaveLoweFile } = await __vitePreload(async () => {
      const { isWantedTeamDaveLoweFormat: isWantedTeamDaveLoweFormat2, parseWantedTeamDaveLoweFile: parseWantedTeamDaveLoweFile2 } = await import("./WantedTeamDaveLoweParser-CzkVhSGq.js");
      return { isWantedTeamDaveLoweFormat: isWantedTeamDaveLoweFormat2, parseWantedTeamDaveLoweFile: parseWantedTeamDaveLoweFile2 };
    }, true ? __vite__mapDeps([130,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "beathovenSynthesizer",
      wtCtx,
      (buf, name) => {
        if (isWantedTeamDaveLoweFormat(buf)) return parseWantedTeamDaveLoweFile(buf instanceof Uint8Array ? buf.buffer : buf, name);
        return null;
      },
      "WantedTeamDaveLoweParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["scn"])) {
    const { isSeanConnollyFormat, parseSeanConnollyFile } = await __vitePreload(async () => {
      const { isSeanConnollyFormat: isSeanConnollyFormat2, parseSeanConnollyFile: parseSeanConnollyFile2 } = await import("./SeanConnollyParser-FMmgxygZ.js");
      return { isSeanConnollyFormat: isSeanConnollyFormat2, parseSeanConnollyFile: parseSeanConnollyFile2 };
    }, true ? __vite__mapDeps([131,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "seanConnolly",
      ctx,
      (buf, name) => {
        const ab = buf instanceof Uint8Array ? buf.buffer : buf;
        if (isSeanConnollyFormat(buf)) return parseSeanConnollyFile(ab, name);
        return null;
      },
      "SeanConnollyParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["tw"])) {
    const { parseUADEFile } = await __vitePreload(async () => {
      const { parseUADEFile: parseUADEFile2 } = await import("./main-BbV5VyEH.js").then((n) => n.ji);
      return { parseUADEFile: parseUADEFile2 };
    }, true ? __vite__mapDeps([0,1,2,3,4,5,6]) : void 0);
    return parseUADEFile(buffer, toUADEPrefixName(originalFileName, ["tw"]), prefs.uade ?? "enhanced", subsong, preScannedMeta);
  }
  if (matchesExt(filename, ["dz"])) {
    const { parseUADEFile } = await __vitePreload(async () => {
      const { parseUADEFile: parseUADEFile2 } = await import("./main-BbV5VyEH.js").then((n) => n.ji);
      return { parseUADEFile: parseUADEFile2 };
    }, true ? __vite__mapDeps([0,1,2,3,4,5,6]) : void 0);
    return parseUADEFile(buffer, toUADEPrefixName(originalFileName, ["dz"]), prefs.uade ?? "enhanced", subsong, preScannedMeta);
  }
  if (matchesExt(filename, ["dns"])) {
    const { parseUADEFile } = await __vitePreload(async () => {
      const { parseUADEFile: parseUADEFile2 } = await import("./main-BbV5VyEH.js").then((n) => n.ji);
      return { parseUADEFile: parseUADEFile2 };
    }, true ? __vite__mapDeps([0,1,2,3,4,5,6]) : void 0);
    return parseUADEFile(buffer, toUADEPrefixName(originalFileName, ["dns"]), prefs.uade ?? "enhanced", subsong, preScannedMeta);
  }
  if (matchesExt(filename, ["vss"])) {
    const { parseUADEFile } = await __vitePreload(async () => {
      const { parseUADEFile: parseUADEFile2 } = await import("./main-BbV5VyEH.js").then((n) => n.ji);
      return { parseUADEFile: parseUADEFile2 };
    }, true ? __vite__mapDeps([0,1,2,3,4,5,6]) : void 0);
    return parseUADEFile(buffer, toUADEPrefixName(originalFileName, ["vss"]), prefs.uade ?? "enhanced", subsong, preScannedMeta);
  }
  if (matchesExt(filename, ["synmod"])) {
    const { parseUADEFile } = await __vitePreload(async () => {
      const { parseUADEFile: parseUADEFile2 } = await import("./main-BbV5VyEH.js").then((n) => n.ji);
      return { parseUADEFile: parseUADEFile2 };
    }, true ? __vite__mapDeps([0,1,2,3,4,5,6]) : void 0);
    return parseUADEFile(buffer, toUADEPrefixName(originalFileName, ["synmod"]), prefs.uade ?? "enhanced", subsong, preScannedMeta);
  }
  if (matchesExt(filename, ["mk2", "mkii"])) {
    const mk2Ctx = { ...ctx, originalFileName: toUADEPrefixName(originalFileName, ["mk2", "mkii"]) };
    const { parseMarkIIFile } = await __vitePreload(async () => {
      const { parseMarkIIFile: parseMarkIIFile2 } = await import("./SimpleAmigaStubParser-CkgiCTxp.js");
      return { parseMarkIIFile: parseMarkIIFile2 };
    }, true ? __vite__mapDeps([132,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "markII",
      mk2Ctx,
      (buf, name) => parseMarkIIFile(buf, name),
      "SimpleAmigaStubParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["scumm"])) {
    const scummCtx = { ...ctx, originalFileName: toUADEPrefixName(originalFileName, ["scumm"]) };
    const { isSCUMMFormat, parseSCUMMFile } = await __vitePreload(async () => {
      const { isSCUMMFormat: isSCUMMFormat2, parseSCUMMFile: parseSCUMMFile2 } = await import("./SCUMMParser-NzJ1aYWl.js");
      return { isSCUMMFormat: isSCUMMFormat2, parseSCUMMFile: parseSCUMMFile2 };
    }, true ? __vite__mapDeps([133,134,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "scumm",
      scummCtx,
      (buf, name) => {
        if (isSCUMMFormat(buf)) return parseSCUMMFile(buf instanceof Uint8Array ? buf.buffer : buf, name);
        return null;
      },
      "SCUMMParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["aps"])) {
    const apsCtx = { ...ctx, originalFileName: toUADEPrefixName(originalFileName, ["aps"]) };
    const { parseAProSysFile } = await __vitePreload(async () => {
      const { parseAProSysFile: parseAProSysFile2 } = await import("./SimpleAmigaStubParser-CkgiCTxp.js");
      return { parseAProSysFile: parseAProSysFile2 };
    }, true ? __vite__mapDeps([132,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "aProSys",
      apsCtx,
      (buf, name) => parseAProSysFile(buf, name),
      "SimpleAmigaStubParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["mok"])) {
    const { parseUADEFile } = await __vitePreload(async () => {
      const { parseUADEFile: parseUADEFile2 } = await import("./main-BbV5VyEH.js").then((n) => n.ji);
      return { parseUADEFile: parseUADEFile2 };
    }, true ? __vite__mapDeps([0,1,2,3,4,5,6]) : void 0);
    return parseUADEFile(buffer, toUADEPrefixName(originalFileName, ["mok"]), prefs.uade ?? "enhanced", subsong, preScannedMeta);
  }
  if (matchesExt(filename, ["sun", "tsm"])) {
    const { isSunTronicFormat, parseSunTronicFile } = await __vitePreload(async () => {
      const { isSunTronicFormat: isSunTronicFormat2, parseSunTronicFile: parseSunTronicFile2 } = await import("./SunTronicParser-BCdHs8Se.js");
      return { isSunTronicFormat: isSunTronicFormat2, parseSunTronicFile: parseSunTronicFile2 };
    }, true ? __vite__mapDeps([135,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "suntronic",
      ctx,
      (buf, name) => {
        if (isSunTronicFormat(buf)) return parseSunTronicFile(buf instanceof Uint8Array ? buf.buffer : buf, name);
        return null;
      },
      "SunTronicParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["dmu"])) {
    const { parseDigitalMugicianFile } = await __vitePreload(async () => {
      const { parseDigitalMugicianFile: parseDigitalMugicianFile2 } = await import("./DigitalMugicianParser-DlVBZ5dm.js");
      return { parseDigitalMugicianFile: parseDigitalMugicianFile2 };
    }, true ? __vite__mapDeps([52,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE("mugician", ctx, (buf, name) => parseDigitalMugicianFile(buf, name), "DigitalMugicianParser", { injectUADE: true });
  }
  if (matchesExt(filename, ["dmu2"])) {
    const { parseDigitalMugicianFile } = await __vitePreload(async () => {
      const { parseDigitalMugicianFile: parseDigitalMugicianFile2 } = await import("./DigitalMugicianParser-DlVBZ5dm.js");
      return { parseDigitalMugicianFile: parseDigitalMugicianFile2 };
    }, true ? __vite__mapDeps([52,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE("mugician", ctx, (buf, name) => parseDigitalMugicianFile(buf, name), "DigitalMugicianParser", { injectUADE: true });
  }
  if (matchesExt(filename, ["mdst"])) {
    const { isJochenHippelSTFormat, parseJochenHippelSTFile } = await __vitePreload(async () => {
      const { isJochenHippelSTFormat: isJochenHippelSTFormat2, parseJochenHippelSTFile: parseJochenHippelSTFile2 } = await import("./JochenHippelSTParser-BPGIIdCV.js");
      return { isJochenHippelSTFormat: isJochenHippelSTFormat2, parseJochenHippelSTFile: parseJochenHippelSTFile2 };
    }, true ? [] : void 0);
    return withNativeThenUADE(
      "jochenHippelST",
      ctx,
      (buf, name) => {
        if (isJochenHippelSTFormat(buf)) return parseJochenHippelSTFile(buf, name);
        return null;
      },
      "JochenHippelSTParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["doda"])) {
    const { isSpecialFXFormat, parseSpecialFXFile } = await __vitePreload(async () => {
      const { isSpecialFXFormat: isSpecialFXFormat2, parseSpecialFXFile: parseSpecialFXFile2 } = await import("./SpecialFXParser-DhpXm7xN.js");
      return { isSpecialFXFormat: isSpecialFXFormat2, parseSpecialFXFile: parseSpecialFXFile2 };
    }, true ? [] : void 0);
    return withNativeThenUADE(
      "specialFX",
      ctx,
      (buf, name) => {
        if (isSpecialFXFormat(buf)) return parseSpecialFXFile(buf, name);
        return null;
      },
      "SpecialFXParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["osp"])) {
    const { isSynthPackFormat, parseSynthPackFile } = await __vitePreload(async () => {
      const { isSynthPackFormat: isSynthPackFormat2, parseSynthPackFile: parseSynthPackFile2 } = await import("./SynthPackParser-jColXfPj.js");
      return { isSynthPackFormat: isSynthPackFormat2, parseSynthPackFile: parseSynthPackFile2 };
    }, true ? [] : void 0);
    return withNativeThenUADE(
      "synthPack",
      ctx,
      (buf, name) => {
        if (isSynthPackFormat(buf, name)) return parseSynthPackFile(buf, name);
        return null;
      },
      "SynthPackParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["gray"])) {
    const { isFredGrayFormat, parseFredGrayFile } = await __vitePreload(async () => {
      const { isFredGrayFormat: isFredGrayFormat2, parseFredGrayFile: parseFredGrayFile2 } = await import("./FredGrayParser-D-r6BhXN.js");
      return { isFredGrayFormat: isFredGrayFormat2, parseFredGrayFile: parseFredGrayFile2 };
    }, true ? [] : void 0);
    return withNativeThenUADE(
      "fredGray",
      ctx,
      (buf, name) => {
        if (isFredGrayFormat(buf, name)) return parseFredGrayFile(buf instanceof Uint8Array ? buf.buffer : buf, name);
        return null;
      },
      "FredGrayParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["jcbo", "jcb", "jb"])) {
    const { parseUADEFile } = await __vitePreload(async () => {
      const { parseUADEFile: parseUADEFile2 } = await import("./main-BbV5VyEH.js").then((n) => n.ji);
      return { parseUADEFile: parseUADEFile2 };
    }, true ? __vite__mapDeps([0,1,2,3,4,5,6]) : void 0);
    const jbFile = toUADEPrefixName(originalFileName, ["jcbo", "jcb", "jb"]);
    const song = await parseUADEFile(buffer, jbFile, "enhanced", subsong, preScannedMeta);
    song.uadeEditableFileData = buffer.slice(0);
    song.uadeEditableFileName = jbFile;
    return song;
  }
  if (matchesExt(filename, ["powt", "pt"])) {
    const { isLaxityFormat, parseLaxityFile } = await __vitePreload(async () => {
      const { isLaxityFormat: isLaxityFormat2, parseLaxityFile: parseLaxityFile2 } = await import("./LaxityParser-CKyQtP08.js");
      return { isLaxityFormat: isLaxityFormat2, parseLaxityFile: parseLaxityFile2 };
    }, true ? [] : void 0);
    return withNativeThenUADE(
      "laxity",
      ctx,
      (buf, name) => {
        if (isLaxityFormat(buf, name)) return parseLaxityFile(buf, name);
        return null;
      },
      "LaxityParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["ea"])) {
    const eaCtx = { ...ctx, originalFileName: toUADEPrefixName(originalFileName, ["ea"]) };
    const { isEarAcheFormat, parseEarAcheFile } = await __vitePreload(async () => {
      const { isEarAcheFormat: isEarAcheFormat2, parseEarAcheFile: parseEarAcheFile2 } = await import("./EarAcheParser-DNo7UusZ.js");
      return { isEarAcheFormat: isEarAcheFormat2, parseEarAcheFile: parseEarAcheFile2 };
    }, true ? __vite__mapDeps([136,137,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "earAche",
      eaCtx,
      (buf, name) => {
        if (isEarAcheFormat(buf)) return parseEarAcheFile(buf, name);
        return null;
      },
      "EarAcheParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["mm4", "sdata"])) {
    const { isMusicMaker4VFormat, parseMusicMaker4VFile } = await __vitePreload(async () => {
      const { isMusicMaker4VFormat: isMusicMaker4VFormat2, parseMusicMaker4VFile: parseMusicMaker4VFile2 } = await import("./MusicMakerParser-Bj1gvTss.js");
      return { isMusicMaker4VFormat: isMusicMaker4VFormat2, parseMusicMaker4VFile: parseMusicMaker4VFile2 };
    }, true ? __vite__mapDeps([138,0,1,2,3,4,5,6]) : void 0);
    const mm4Base = getBasename(filename);
    const mm4Dot = mm4Base.lastIndexOf(".");
    const mm4Ctx = mm4Dot > 0 && !mm4Base.startsWith("mm4.") && !mm4Base.startsWith("sdata.") ? { ...ctx, originalFileName: `${mm4Base.slice(mm4Dot + 1)}.${mm4Base.slice(0, mm4Dot)}` } : ctx;
    return withNativeThenUADE(
      "musicMaker4V",
      mm4Ctx,
      (buf, name) => {
        if (isMusicMaker4VFormat(buf, name)) return parseMusicMaker4VFile(buf, name);
        return null;
      },
      "MusicMakerParser/4V",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["mm8"])) {
    const { isMusicMaker8VFormat, parseMusicMaker8VFile } = await __vitePreload(async () => {
      const { isMusicMaker8VFormat: isMusicMaker8VFormat2, parseMusicMaker8VFile: parseMusicMaker8VFile2 } = await import("./MusicMakerParser-Bj1gvTss.js");
      return { isMusicMaker8VFormat: isMusicMaker8VFormat2, parseMusicMaker8VFile: parseMusicMaker8VFile2 };
    }, true ? __vite__mapDeps([138,0,1,2,3,4,5,6]) : void 0);
    const mm8Base = getBasename(filename);
    const mm8Dot = mm8Base.lastIndexOf(".");
    const mm8Ctx = mm8Dot > 0 && !mm8Base.startsWith("mm8.") ? { ...ctx, originalFileName: `${mm8Base.slice(mm8Dot + 1)}.${mm8Base.slice(0, mm8Dot)}` } : ctx;
    return withNativeThenUADE(
      "musicMaker8V",
      mm8Ctx,
      (buf, name) => {
        if (isMusicMaker8VFormat(buf, name)) return parseMusicMaker8VFile(buf, name);
        return null;
      },
      "MusicMakerParser/8V",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["mon"]) && !matchesExt(filename, ["mon_old"])) {
    const monCtx = { ...ctx, originalFileName: toUADEPrefixName(originalFileName, ["mon"]) };
    const { isManiacsOfNoiseFormat, parseManiacsOfNoiseFile } = await __vitePreload(async () => {
      const { isManiacsOfNoiseFormat: isManiacsOfNoiseFormat2, parseManiacsOfNoiseFile: parseManiacsOfNoiseFile2 } = await import("./ManiacsOfNoiseParser-D_OoJ8DT.js");
      return { isManiacsOfNoiseFormat: isManiacsOfNoiseFormat2, parseManiacsOfNoiseFile: parseManiacsOfNoiseFile2 };
    }, true ? [] : void 0);
    return withNativeThenUADE(
      "maniacsOfNoise",
      monCtx,
      (buf, name) => parseManiacsOfNoiseFile(buf, name),
      "ManiacsOfNoiseParser",
      { isFormat: (b) => isManiacsOfNoiseFormat(b, filename), injectUADE: true }
    );
  }
  if (matchesExt(filename, ["ptcop", "pttune"])) {
    const { isPxtoneFormat, parsePxtoneFile } = await __vitePreload(async () => {
      const { isPxtoneFormat: isPxtoneFormat2, parsePxtoneFile: parsePxtoneFile2 } = await import("./PxtoneParser-B2hqtFor.js");
      return { isPxtoneFormat: isPxtoneFormat2, parsePxtoneFile: parsePxtoneFile2 };
    }, true ? [] : void 0);
    if (isPxtoneFormat(buffer)) {
      return parsePxtoneFile(originalFileName, buffer);
    }
  }
  if (matchesExt(filename, ["org"])) {
    const { isOrganyaFormat, parseOrganyaFile } = await __vitePreload(async () => {
      const { isOrganyaFormat: isOrganyaFormat2, parseOrganyaFile: parseOrganyaFile2 } = await import("./OrganyaParser-DfOPCnJy.js");
      return { isOrganyaFormat: isOrganyaFormat2, parseOrganyaFile: parseOrganyaFile2 };
    }, true ? [] : void 0);
    if (isOrganyaFormat(buffer)) {
      return parseOrganyaFile(buffer, originalFileName);
    }
  }
  if (matchesExt(filename, ["eup"])) {
    const { isEupFormat, parseEupFile } = await __vitePreload(async () => {
      const { isEupFormat: isEupFormat2, parseEupFile: parseEupFile2 } = await import("./EupminiParser-DHNojyBz.js");
      return { isEupFormat: isEupFormat2, parseEupFile: parseEupFile2 };
    }, true ? [] : void 0);
    if (isEupFormat(buffer)) {
      return parseEupFile(originalFileName, buffer);
    }
  }
  if (matchesExt(filename, ["ixs"])) {
    const { isIxsFormat, parseIxsFile } = await __vitePreload(async () => {
      const { isIxsFormat: isIxsFormat2, parseIxsFile: parseIxsFile2 } = await import("./IxalanceParser-BSieCown.js");
      return { isIxsFormat: isIxsFormat2, parseIxsFile: parseIxsFile2 };
    }, true ? [] : void 0);
    if (isIxsFormat(buffer)) {
      return parseIxsFile(originalFileName, buffer);
    }
  }
  if (matchesExt(filename, ["psy"])) {
    const { isPsycleFormat, parsePsycleFile } = await __vitePreload(async () => {
      const { isPsycleFormat: isPsycleFormat2, parsePsycleFile: parsePsycleFile2 } = await import("./CpsycleParser-UHkw_hDi.js");
      return { isPsycleFormat: isPsycleFormat2, parsePsycleFile: parsePsycleFile2 };
    }, true ? [] : void 0);
    if (isPsycleFormat(buffer)) {
      return parsePsycleFile(buffer, originalFileName);
    }
  }
  if (matchesExt(filename, ["sc68", "sndh", "snd"])) {
    const { isSc68Format, parseSc68File } = await __vitePreload(async () => {
      const { isSc68Format: isSc68Format2, parseSc68File: parseSc68File2 } = await import("./Sc68Parser-CKrE9RWu.js");
      return { isSc68Format: isSc68Format2, parseSc68File: parseSc68File2 };
    }, true ? [] : void 0);
    if (isSc68Format(buffer)) {
      return parseSc68File(originalFileName, buffer);
    }
  }
  if (matchesExt(filename, ["pt3", "pt2", "pt1", "stc", "st1", "st3", "stp", "vtx", "psg", "psm", "sqt", "psc", "asc", "gtr", "ftc", "ayc", "ts", "cop", "tfc", "tfd", "tf0", "pdt", "chi", "str", "dst", "dmm", "et1"])) {
    const { isZxtuneFormat, parseZxtuneFile } = await __vitePreload(async () => {
      const { isZxtuneFormat: isZxtuneFormat2, parseZxtuneFile: parseZxtuneFile2 } = await import("./ZxtuneParser-C5YuRBZE.js");
      return { isZxtuneFormat: isZxtuneFormat2, parseZxtuneFile: parseZxtuneFile2 };
    }, true ? [] : void 0);
    if (isZxtuneFormat(buffer)) {
      return parseZxtuneFile(originalFileName, buffer);
    }
  }
  if (matchesExt(filename, ["glue", "gm"])) {
    const { isGlueMonFormat, parseGlueMonFile } = await __vitePreload(async () => {
      const { isGlueMonFormat: isGlueMonFormat2, parseGlueMonFile: parseGlueMonFile2 } = await import("./GlueMonParser-aQFXPLH-.js");
      return { isGlueMonFormat: isGlueMonFormat2, parseGlueMonFile: parseGlueMonFile2 };
    }, true ? [] : void 0);
    return withNativeThenUADE(
      "glueMonParser",
      ctx,
      (buf, name) => {
        if (isGlueMonFormat(buf)) return parseGlueMonFile(buf, name);
        return null;
      },
      "GlueMonParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["dh"])) {
    const { isDavidHanneyFormat, parseDavidHanneyFile } = await __vitePreload(async () => {
      const { isDavidHanneyFormat: isDavidHanneyFormat2, parseDavidHanneyFile: parseDavidHanneyFile2 } = await import("./DavidHanneyParser-CTCgWV1F.js");
      return { isDavidHanneyFormat: isDavidHanneyFormat2, parseDavidHanneyFile: parseDavidHanneyFile2 };
    }, true ? [] : void 0);
    return withNativeThenUADE(
      "davidHanney",
      ctx,
      (buf, name) => {
        if (isDavidHanneyFormat(buf)) return parseDavidHanneyFile(buf instanceof Uint8Array ? buf.buffer : buf, name);
        return null;
      },
      "DavidHanneyParser",
      { injectUADE: true }
    );
  }
  if (matchesExt(filename, ["aam"])) {
    const aamCtx = { ...ctx, originalFileName: toUADEPrefixName(originalFileName, ["aam"]) };
    const { parseArtAndMagicFile } = await __vitePreload(async () => {
      const { parseArtAndMagicFile: parseArtAndMagicFile2 } = await import("./SimpleAmigaStubParser-CkgiCTxp.js");
      return { parseArtAndMagicFile: parseArtAndMagicFile2 };
    }, true ? __vite__mapDeps([132,0,1,2,3,4,5,6]) : void 0);
    return withNativeThenUADE(
      "artAndMagic",
      aamCtx,
      (buf, name) => parseArtAndMagicFile(buf, name),
      "SimpleAmigaStubParser",
      { injectUADE: true }
    );
  }
  {
    const uadeResult = await tryUADEPrefixParse(buffer, filename, originalFileName, prefs, subsong, preScannedMeta);
    if (uadeResult) return uadeResult;
  }
  {
    const pcResult = await tryPCTrackerParse(buffer, filename, originalFileName);
    if (pcResult) return pcResult;
  }
  return null;
}
export {
  tryRouteFormat
};
