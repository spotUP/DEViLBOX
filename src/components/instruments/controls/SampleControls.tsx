/**
 * SampleControls - Control panel for sample-based instruments
 *
 * Re-exports SampleEditor as SampleControls for use in the unified editor.
 * The SampleEditor already has a clean component structure that works as controls.
 */

export { SampleEditor as SampleControls } from '../SampleEditor';
export { SampleEditor as default } from '../SampleEditor';
