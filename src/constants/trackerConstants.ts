/**
 * Tracker Constants - Default values and limits for the pattern editor
 */

export const DEFAULT_PATTERN_LENGTH = 64;
export const DEFAULT_NUM_CHANNELS = 4;

export const MIN_PATTERN_LENGTH = 1;
export const MAX_PATTERN_LENGTH = 256;

export const MIN_CHANNELS = 1;
export const MAX_CHANNELS = 16;

export const DEFAULT_BPM = 125;
export const MIN_BPM = 32;
export const MAX_BPM = 999;

export const DEFAULT_LPB = 4; // Lines per beat
