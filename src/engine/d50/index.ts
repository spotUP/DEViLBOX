/**
 * Roland D-50 Linear Arithmetic Synthesizer Module
 *
 * Based on the Roland D-50 (1987) which uses the Roland SA chip
 * for Linear Arithmetic synthesis (PCM + LA).
 */

export { D50Synth, D50_FACTORY_PRESETS } from './D50Synth';
export type { D50Patch, D50Partial } from './D50Synth';
export { D50Structure, D50WaveGroup } from './D50Synth';
