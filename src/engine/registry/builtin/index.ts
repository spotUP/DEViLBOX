/**
 * Built-in synth registrations — imported eagerly at startup.
 *
 * Each file registers its synths with SynthRegistry as a side effect.
 */

import './tone';
import './sampler';
import './tb303';
import './furnace';
import './supercollider';
