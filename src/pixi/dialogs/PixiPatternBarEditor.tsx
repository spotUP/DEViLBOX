/**
 * PixiPatternBarEditor — modal dialog for editing pattern bar parameters.
 * Stub — placeholder until full implementation.
 */

import React from 'react';

interface Props {
  isOpen: boolean;
  onClose: () => void;
}

export const PixiPatternBarEditor: React.FC<Props> = ({ isOpen }) => {
  if (!isOpen) return null;
  return null; // Stub — renders nothing for now
};
